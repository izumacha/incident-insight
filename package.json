{
  "name": "incident-insight-frontend",
  "private": true,
  "version": "0.1.0",
  "description": "Frontend type-checking and TypeScript build tooling for the IncidentInsight ASP.NET Core MVC app. tsc compiles src/IncidentInsight.Web/Scripts/**/*.ts into src/IncidentInsight.Web/wwwroot/js/ via the MSBuild CompileTypeScript target.",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
