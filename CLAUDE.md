# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

> このファイルの **§4 以降（共通規約 ＋ 付録）は原本テンプレート `izumacha/claude-code-rules` の
> `CLAUDE.md` と同期**している。共通規約を変更するときは、**まず原本を改訂してから**各リポジトリへ
> 反映すること（このファイルで共通規約だけを勝手に書き換えない）。§1〜§3 は本リポジトリ固有の内容。

---

## 1. プロジェクト概要

IncidentInsight は医療インシデント管理システム（ASP.NET Core 8 MVC + EF Core 9）。報告 → なぜなぜ分析（5 Whys）→ 予防策立案 → 完了 → 効果評価 → 再発検知までのライフサイクルを扱う。UI 文字列とドメイン語彙は日本語（`重症度`, `なぜなぜ分析`, `再発防止策` 等）。

アプリは**DB プロバイダ非依存**: SQLite（既定・単一ファイル）/ SQL Server（オンプレ Windows）/ PostgreSQL（Linux・クラウド・Supabase 等）を `Database:Provider` 設定で実行時切替する。モデル・クエリにプロバイダ固有 SQL／列型を持ち込まないため、同一コードベースでどこへでも配備できる。

## 2. コマンド

```bash
# ビルド（ソリューション全体）
dotnet build

# Web アプリ起動（初回起動で SQLite DB 作成＋シード）
# 待受 URL はハードコードせず、起動ログの「Now listening on:」か launchSettings.json の
# applicationUrl（例: http://localhost:32562 / http://localhost:5093）を見る。
dotnet run --project src/IncidentInsight.Web/IncidentInsight.Web.csproj

# 全テスト（xUnit）
dotnet test

# 単一テストクラス / メソッド
dotnet test --filter "FullyQualifiedName~IncidentsControllerTests"
dotnet test --filter "FullyQualifiedName=IncidentInsight.Tests.Controllers.IncidentsControllerTests.Create_Post_WithoutMeasures_ReturnsCreateView_AndDoesNotSaveIncident"

# モデル変更後の EF Core マイグレーション追加（起動時 Database.Migrate() で自動適用）
# 注: コミット済みマイグレーションは既定プロバイダ(SQLite)向け。SQL Server / PostgreSQL 本番に
#     切替える場合は Migrations/ を削除し Database__Provider と接続文字列を設定して再生成する。
dotnet ef migrations add <MigrationName> --project src/IncidentInsight.Web

# ローカルログイン用デモアカウントのシード（Development のみ。パスワードはコミットしない）
dotnet user-secrets init --project src/IncidentInsight.Web
dotnet user-secrets set "SeedAccounts:AdminPassword" "AdminPass1" --project src/IncidentInsight.Web
dotnet user-secrets set "SeedAccounts:RiskManagerPassword" "RiskPass1" --project src/IncidentInsight.Web
```

Dev パスワードポリシー: 8 文字以上・大文字・数字。Prod: 12 文字以上・非英数字含む（`Program.cs`）。`SeedAccounts` は Development のみシードし、空なら警告ログを出してスキップする。

## 3. アーキテクチャ

### リクエストパイプライン / 起動（`Program.cs`）

- `ApplicationDbContext` は `Database:Provider`（`sqlite` | `sqlserver` | `postgres`）で実行時プロバイダ切替。接続文字列は `ConnectionStrings:DefaultConnection`。`UseSqlite` / `UseSqlServer` / `UseNpgsql` を使い分け、移行はコード変更不要（設定のみ）。
- `AuditSaveChangesInterceptor` を `DbContext` に登録（プロバイダ中立）。`Incident` / `CauseAnalysis` / `PreventiveMeasure` の Add/Modify/Delete ごとに `AuditLogs` 行を書き、Modified では `ConcurrencyToken` Guid を回転させる。
- ASP.NET Core Identity を `ApplicationUser` ＋ 3 ロール（`AppRoles`: `Admin` / `RiskManager` / `Staff`）で構成。Cookie 認証は `/Account/Login` リダイレクト、8h スライディング、5 回失敗でロックアウト。
- 起動時（スコープ内）: `db.Database.Migrate()` → `DbSeeder.Seed(db)`（原因分類マスタ・冪等・全環境）→ `DbSeeder.SeedDemoData(db, clock)`（デモインシデント・Development のみ）→ `IdentitySeeder.SeedAsync(...)`（ロールは常時、デモ管理者/RM は Development かつ `SeedAccounts` パスワード有時のみ）。

### DB プロバイダ・マトリクス

| 配備先 | `Database:Provider` | 接続文字列の例 |
|---|---|---|
| 単一クリニック / dev（既定） | `sqlite` | `Data Source=incident_insight.db` |
| オンプレ病院（Windows / 既存 SQL Server） | `sqlserver` | `Server=...;Database=IncidentInsight;Trusted_Connection=True;TrustServerCertificate=True;` |
| Linux / 自前 Postgres | `postgres` | `Host=...;Database=incidentinsight;Username=...;Password=...` |
| マネージド（Supabase / Azure DB / Aurora） | `postgres` | プロバイダ標準の Postgres 接続文字列 |

設定は `appsettings*.json` か環境変数（`Database__Provider`, `ConnectionStrings__DefaultConnection`）。

### ドメインモデル（`src/IncidentInsight.Web/Models/`）

4 つの集約に `OnModelCreating` でカスケードを設定:

- **`Incident`**（ルート）— 発生メタデータを保持。重症度コード（`Level0`…`Level5`）・部署一覧・インシデント種別一覧は `Incident` クラスの `static readonly` 辞書/配列が**唯一の真実の源**（ドロップダウン/ラベル）。`SeverityLabel` / `SeverityColor` / `MeasureStatusSummary` 等の計算プロパティを Razor で直接使う。
- **`CauseAnalysis`** — 5 Whys を独立列 `Why1`..`Why5` で保持（深さ別検索のため）。常に 1 つの `CauseCategory` に紐づく。`DeepestWhy` / `WhyDepth`。
- **`CauseCategory`** — `ParentId` による自己参照階層。`BuildCauseCategoryOptionsAsync()`（`Controllers/Internal/IncidentControllerHelpers.cs`）が leaf を parent ごとに `<optgroup>` で出力。これをバイパスするカテゴリピッカーを作らない。
- **`PreventiveMeasure`**（ドメイン最重要）— `Planned → InProgress → Completed` と完了後フィールド（`EffectivenessRating` 1–5, `EffectivenessNote`, `EffectivenessReviewedAt`, `RecurrenceObserved`）。`IsOverdue`（`DueDate < DateTime.Today && Status != "Completed"`）の意味を一貫させる。
- **`AuditLog`** — 規制対応の監査証跡。`AuditSaveChangesInterceptor` が自動で書く。**このテーブルに直接書かない**（インターセプタが唯一の源）。

`Incident` / `CauseAnalysis` / `PreventiveMeasure` は `ConcurrencyToken`（Guid, `[ConcurrencyCheck]`）を持つ。**Edit POST 契約**: `FindAsync` で再読込後、クライアントの編集前トークンを `OriginalValue` に明示ピンしてから保存し、`DbUpdateConcurrencyException` を捕捉する:

```csharp
_db.Entry(entity).Property(nameof(Entity.ConcurrencyToken)).OriginalValue = vm.ConcurrencyToken;
try { await _db.SaveChangesAsync(); }
catch (DbUpdateConcurrencyException) { TempData["Warning"] = "..."; return ...; }
```

トークンは hidden field（`<input type="hidden" asp-for="ConcurrencyToken" />` 等）で round-trip する。インデックス: `Incident(OccurredAt)`, `Incident(Department, IncidentType)`, `PreventiveMeasure(Status, DueDate)`, `CauseCategory(ParentId, DisplayOrder)`, `AuditLog(ChangedAt)`, `AuditLog(EntityName, EntityKey)`。

### コントローラ / 横断パターン

- 全コントローラ(`AccountController` 含む)がクラス既定 `[Authorize]`(fail-closed)。`AccountController` は `Login` / `AccessDenied` / `Logout` のみアクション単位の `[AllowAnonymous]`(`Logout` はクッキー失効後の 405 デッドエンド防止)。
- インシデントのライフサイクルは `/Incidents/...` 配下の**3 コントローラ**に分割:
  - `IncidentsController` — Index/Details/Create/Edit/Delete。Create は `HasAtLeastOneValidMeasure`（業務必須）を強制。`Edit` で親フォーム検証前にサブフォームキーを `ModelState.Remove` で除去。
  - `CauseAnalysesController` — `AddCauseAnalysis` / `EditCauseAnalysis` / `DeleteCauseAnalysis`。`[Route("Incidents/[action]/{id?}")]` で URL を維持し、`View("~/Views/Incidents/EditCauseAnalysis.cshtml", vm)` を再利用。
  - `IncidentMeasuresController` — `AddMeasure` / `CompleteMeasure` / `RateMeasure`（詳細ページからのインライン操作）。kanban 側の `Create`/`Edit`/`Complete`/`Review`/`UpdateStatus`/`Delete` は `PreventiveMeasuresController`。
- 共有ヘルパー（カテゴリドロップダウン生成・リソースポリシー評価）は `Controllers/Internal/IncidentControllerHelpers.cs`。**業務ルールはここに置かない**（`HasAtLeastOneValidMeasure` 等は所有コントローラに残す）。発生部署の絞り込み値の解決（許可リスト外の値を補完するか採用しないか）は `Controllers/Internal/DepartmentFilterResolver.cs` に集約し、`/Incidents` と `/Analytics` が共有する。**一覧のフリーワード検索は述語ごと `Controllers/Internal/KeywordSearchPredicate.cs` から出す**（`query.Where(KeywordSearchPredicate.Matching<T>(keyword, x => x.Col, ...))`）— 突き合わせる 2 辺（列側の `ToUpper()` と キーワード側の `ToUpperInvariant()`）はペアで揃ってはじめて正しく、片方を呼び出し側に書ける形にすると**書き忘れても全テストが緑のまま PostgreSQL 配備でだけ 0 件になる**（issue #188。集約前の実測で `/Incidents` と `/PreventiveMeasures` は列側を落としても全件緑だった）。**新しい一覧検索はこの述語を経由することだけを守る**（ソース走査の検出網は置けない。理由は同クラスの「残る境界 4」が正本で、**「必要な書き方まで咎めるから」ではなく「捕まえたい形に目印が無いから」** — 実際に壊れるのは大文字化を 1 つも書かない `Contains` なので、`ToUpper` を禁じる走査は「惜しい」書き方しか拾えない）。**絞り込み入力の規則そのものは `Models/Validation/SearchFilter.cs` が正本**（方式の表・空判定・「採用しなかった値をどう伝えるか」の理由がすべてそこにある。ここにファイルを書き並べると一覧の方が先に古くなるので、参照 1 本に寄せている）。**「受け取ったのに採用しなかった」絞り込み値は、方式（補完する／採用しない）にかかわらず必ず旗を返して「適用していません」を伝える**。判定の置き場所は「いま何画面が使うか」で決める（§6 の「2〜3 箇所目で共通化」）: 型として読めなかった値は共有の `MalformedFilterValueResolver`、enum の定義に無い値（`?severity=99` / `?status=99`）は共有の `UnlistedEnumFilterResolver`、**文字列の許可リストで閉じた値（`/AuditLogs` の `?entityName=` / `?operation=`）は当面その画面の private な `ResolveListedValue`**（利用側が 1 画面 2 箇所なので共有ヘルパーへ出していない。2 画面目が同じ形を必要としたら `Controllers/Internal/` へ移す）。**どの置き場所でも「採用した値」と「採用しなかったか」を 1 つの record struct で返す形をそろえる** — 旗の一覧をコントローラのソースから `… = ….Ignored` という代入の形で導いている検出網が拾えなくなるため。**「閉じた語彙だから黙ってよい」という規則は無い**（issue #220。方式と伝え方は別の軸で、混同すると同じ画面の中で答えが割れる）。**型付き絞り込みを受ける画面は 4 つとも読めない値の手当てが入っている**（issue #207）。伝え先だけが画面ごとに違い、`/Incidents` と `/AuditLogs` は ViewModel、`/PreventiveMeasures` は ViewBag（ViewModel を持たない）、`/Analytics` は JSON のキー `malformedFilterIgnored`（注意書きを出す場所が無い）。注意書きの枠は共有パーシャル `Views/Shared/_FilterIgnoredNotice.cshtml` で、**同じ理由の文面は画面をまたいで一字一句そろえる**（言い回しが違うと利用者は別の出来事だと受け取る）。**この 2 つの検出網が見る「対象画面の一覧」は、一覧そのものとは独立な手がかりで網羅を照合する**（issue #224）: 旗を ViewModel で運ぶ画面の表（`ViewModelFlagScreens`）は**アセンブリ上で `*FilterIgnored` の bool を宣言している型**と、注意書きを出す画面の表（`IgnoredFilterNoticeScreens`）は**ビューのソースに実在する `@if (Model./ViewBag.*FilterIgnored)` の出し分け**と突き合わせる。手書きの包含リストのままだと、4 画面目を足して登録を忘れてもその画面だけが黙って照合から外れ（登録済みどうしは一致し続けるので）**全件緑のまま**、誰も立てない旗や言い回しの割れた文面が出荷される。**旗を分ける／まとめるの基準は「採用しなかった理由が同じかどうか」**（`/AuditLogs` の `entityName` と `operation` は理由が同一なので旗 1 つ、「読めない」と「選べる値ではない」は理由が違うので別の旗・別の見出し。見出しの重複は `AuditLogsIndexView_GivesEachIgnoredFilterNoticeItsOwnHeading` 等が落とす — 2 つの旗は同時に立ちうるので、同じ見出しだと二重描画の不具合に見える）。**新しい画面が期間の絞り込みを受けたときの付け忘れは `UnlistedFilterValuePolicyTests.MalformedFilterScreens_CoverEveryActionThatAcceptsADateRangeFilter` が「アプリ全体のアクションが受ける `DateTime?` の引数」から導いて落とす**（同テストの表が「どの引数を、どの伝え先で覆っているか」の唯一の真実の源。手がかりを `DateTime?` に絞っているのは、「読めなければ化ける引数すべて」へ広げると `Details(long id)` のようなルートのキーまで巻き込んで直しようの無い要求になるため。`int?` の範囲指定など別の型で絞り込みを足す人が手がかりを広げる）。**enum の絞り込みを新しい画面へ足すときは必ず `UnlistedEnumFilterResolver` を通す** — 通し忘れると絞り込みが実際に掛かって 0 件になる一方 `<select>` は「（全て）」を指し、再送信で絞り込みが黙って解除される。付け忘れ自体は `UnlistedFilterValuePolicyTests.EnumFilterScreens_CoverEveryActionThatAcceptsAnEnumFilter` が「アプリ全体のアクションが受ける **enum の引数**（null 許容かどうかを問わない）」から導いて落とす。同テストの表は守り方を 2 種類に分けており、**絞り込みなら `UnlistedEnumFilterResolver`、保存を伴う POST なら自前のゲート**（`UpdateStatus` がその例。「採用しない」では済まず未定義値を保存させない必要がある）のどちらかを必ず選ぶ。**enum のアクション引数は、守り方の種類によらず `Nullable<T>` で受けて `null` を弾く** — 非 null 許容で受けると値が届かなかったときに `default(T)` へ黙って化け、**それは必ず定義済みの値**なので保存側の `Enum.IsDefined` ゲートも、絞り込み側の `UnlistedEnumFilterResolver`（`Enum.IsDefined` で採用を決める）も素通りする。しかも「そもそも送られてこない」形は `ModelState` にキーすら残さない（`IsValid` は `true` のまま）ので `ModelState` を見るガードでも捕まえられない（どちらも実測。`UndefinedEnumModelBindingTests` が固定）。壊れ方は守り方で違い、**保存側は「送っていない既定値が保存される」**（実際 `UpdateStatus` はこれで、完了済みの対策が黙って `Planned` へ差し戻され完了後データが全消去されたうえ「更新しました」と表示されていた）、**絞り込み側は「送っていない既定値で絞り込まれる」**（`?status=99` が「Planned で絞った一覧」になる）。**この規則のうち「`Nullable<T>` で受ける」側は `EnumActionParameters_AreNullable_SoAnUnboundValueCannotBecomeADefinedDefault` が機械的に落とす**（issue #233）— 以前は表の値（`Filter` / `OwnGate`）が**素の文字列でどの検査も読んでおらず**、非 null 許容の enum を受ける保存 POST を 1 つ足して `OwnGate` として登録すると**全件緑のまま通り、テスト件数すら変わらなかった**（実測 854 → 854）。守り方は enum 型で持ち、違反の一覧は**表ではなく導出から**作る（表を基準にすると、載せ忘れた引数がこの検査からも同時に外れる）。表の値は失敗文言で「どちらの壊れ方か」を名指しするために読む。**除外表は置いていない** — 非 null 許容が正当な enum 引数は現時点で 1 つも無く、空の除外表が「登録するだけで黙らせられる口」になった実例があるため（§6）。判定そのものは純粋関数へ出し、`NonNullableEnumArguments_ReportsOnlyNonNullable_AndNamesHowItBreaks` が合成入力で挙動を固定する（実在する引数がたまたま全部 `Nullable<T>` である限り、判定を潰しても緑のままになるため）。**残っている境界: 「受けたあと `null` を弾く」側に検出網は無い** — `Nullable<T>` で受けておきながら `status.GetValueOrDefault()` / `status ?? MeasureStatus.Planned` と書くと、issue #233 とまったく同じ「送っていない既定値が保存される」形が再現するのに全件緑になる（弾いているかを署名から判定する手掛かりが無い）。**ペアの後半は規約とレビューで守る**（前半が機械化されたことを「規則全体が機械化された」と読むと、この半分を書き忘れても検出されると誤解する）。**走査が引数を拾う単位は「キーと引数の型」にする** — 以前はキーだけで畳んで `First()` を採っていたため、同じ (アクション名, 引数名) で型だけが違うオーバーロード（GET/POST 対がこの形）が 1 件に潰れ、どちらが見られるかが `Type.GetMethods` の返す順＝**宣言順**で決まっていた。実測でも、非 null 許容の `MeasureStatus` を受ける POST を既存の `Nullable<T>` の**後ろ**へ足すと**856 件すべて緑のまま通り**（**前**へ置くと落ちる）、テスト件数も変わらなかった。畳み方は純粋関数 `CollapseDuplicateActionParameters` に出し、`CollapseDuplicateActionParameters_KeepsOverloadsThatDifferOnlyByType` が合成入力で固定する（実在の署名にキー衝突が 1 つも無い限り、キーだけへ戻す退行は全件緑のままになるため）。キーだけで足りる網羅ガード向けの重複は `ActionParametersInTheApp` 側の `Distinct` が吸収する。**なお「定義に無い enum 値は素通しで束縛される」という記述は誤り**で、既定では ASP.NET Core の `EnumTypeModelBinder` が束縛エラーを積む（**無効化する `MvcOptions` の設定は存在せず**、フラグは公式に "currently ignored"。1 段目は引数 1 つ単位で外せる（`MvcOptions.ModelBinderProviders` への独自 binder provider の差し込みに加え、`[ModelBinder(typeof(…))]` / `[FromBody]` が使う provider は既定の並びで `EnumTypeModelBinderProvider` より**前**にいる）ため、門番は fail-safe として残す。挙動は `UndefinedEnumModelBindingTests` が固定する。issue #215）。**「読めない値」の手当てが要る引数は `Nullable<T>` だけではない** — 束縛に失敗したとき黙って化ける先は `Nullable<T>` なら `null`、**それ以外の値型なら `default(T)`** で、どちらも失敗の事実は `ModelState` にしか残らない。以前は導出も解説も `Nullable<T>` しか見ていなかったため、`int page = 1` は**それを見張るはずの Theory のケースに入りようがなかった**（issue #211）。`UnlistedFilterValuePolicyTests` の導出（`IsUnreadableProne`）は**値型かつ「文字列から変換できる型」**（＝MVC の `SimpleTypeModelBinder` が効く範囲。`TypeDescriptor` の変換器で判定）を拾い、意図的に外す引数は**理由付きの表 `MalformedFilterExemptions`**（**画面（コントローラ名）× URL 上の名前**がキー）にだけ登録する — 表に無い引数は Theory が拾って落ちるので、次に同じ形の引数（`bool overdueOnly` 等）を足す人は「渡すか、除外するか」を必ず一度決めることになる（実測でも、以前の導出では `overdueOnly` を足しても**全件緑のまま通った**）。**条件に「既定値があること」を足してはいけない** — 既定値を書かなくても束縛失敗時は `default(T)` になるので、足すと「既定値を書かなかった引数だけが黙って外れる」同じ穴をより狭い形で作り直すことになる。逆に**「文字列から変換できる型」で絞るのは必須**で、専用バインダを持つ値型（`CancellationToken ct = default` 等）まで巻き込むと、変換エラーが積まれないため Theory が**直しようの無い**要求を出し、残る道は「絞り込みでない引数を解決処理へ渡す」か「除外表を広げる」だけになる（実行不能な指示を出す検出網はいずれ緩められる）。現在の登録は `page` の 2 件（`/Incidents` と `/AuditLogs`）で、外す理由は「ページ番号は絞り込みではなく、読めない値も範囲外の値も `Math.Clamp` で最寄りの有効なページへ丸め、着地したページはページャが実際に表示している＝絞り込みのように『送ったのに効いていない』状態が画面から見えなくなることが無い」。**キーに画面を含めるのは、除外の根拠が画面ごとに成り立ったり成り立たなかったりするから**（`page` の根拠は `Math.Clamp` で丸めていることで、それを持つのはこの 2 画面だけ）。以前は URL 上の名前だけをキーにしていたため、`/PreventiveMeasures` に同名の引数を足すだけで**根拠が成り立たないまま除外が効いた**うえ、広がったのは既存エントリの適用範囲なので**差分にもテスト件数にも現れなかった**（issue #224）。画面込みなら 3 画面目の除外は必ず 1 行の差分として現れ、下の「エントリが増える差分はレビューで確認する」が効く。表には 3 つの検査が掛かる（キーの (画面, 引数) がその画面に実在する / 理由が空でない / **`Nullable<T>` は登録できない** — 登録できると `severity` を 1 行足すだけで注意書きを黙らせられるため）。このうち前 2 つ（実在する / `Nullable<T>` を隠していない）は**画面ごとに突き合わせる**（名前だけで照合すると、`page` を持つ画面が 1 つでもあれば「どの画面に登録しても実在する」ことになり、根拠の無い画面への登録を素通りさせる）。画面のキーは**完全修飾名**にする — 単純名だと、名前空間だけが違う同名のコントローラ（MVC の Areas がこの形）が同じバケットに畳まれ、既存エントリの除外が新しい画面にも黙って効く。**この綴りは除外表だけでなく、網羅ガードが突き合わせる導出のキーにも同じものを使う**（`ActionParameterKey` 経由で `ScreenKeyOf` 1 か所に寄せてある）— 導出側が単純名のままだと、Areas の同名コントローラが `Distinct` で既存の1 件へ畳まれ、その画面は解決処理を 1 行も通していないのに「手当て済み」として数えられる。**塞いだはずの穴がガードを支える導出側に残る形**で、痕跡は差分にもテスト件数にも出ない（issue #227。実測で全件緑のまま通った）。導出が完全修飾名であること自体は `ActionParameterKeys_AreFullyQualified` が、導出とは独立な手がかり（コントローラが名乗る名前空間の根）で照合する。**ただし「絞り込みかどうか」は署名から判定できないので、この表は最後は人が見るエスケープハッチ**（`LengthGovernanceExclusions` と同じ扱い。**エントリが増える差分は理由の妥当性をレビューで必ず確認する**）。導出は型付き絞り込みを受ける画面すべて（一覧 3 画面 `/Incidents` ・ `/PreventiveMeasures` ・ `/AuditLogs` と `/Analytics`）で共有し、拾う名前は C# の引数名ではなく**モデルバインドがキーに使う URL 上の名前**にする（`[FromQuery(Name = ...)]` で別名を付けたときの食い違いを検出できるようにするため）。
- 移動したアクションの Tag Helper は `asp-controller="CauseAnalyses"` / `asp-controller="IncidentMeasures"` を指定する（URL は `/Incidents/...` のままだがルート解決に必要）。
- `AnalyticsController` は Chart.js 用 JSON（`{ labels, data }` 形状を変えない。キーの**追加**は可で、`colors` / `recurrenceStats` / `departmentFilterIgnored` / `malformedFilterIgnored` がその例）。期間の絞り込み（`?dateFrom=` / `?dateTo=`）を受けるエンドポイントは `MalformedFilterValueResolver` を通し、読めなかったときは `malformedFilterIgnored: true` を添える（そうしないと「期間を絞ったかのような全期間のグラフ」が旗も無しで返り、「その期間は 0 件だった」と読めてしまう。issue #207）。`?department=` は `DepartmentFilterResolver` を通し、採用しなかったときは `departmentFilterIgnored: true` を添える（`/Incidents` が画面へ注意書きを出すのと同じ理由。この画面はまだ部署の絞り込み UI を持たないため伝え先が JSON になっている）。**運用上の注意**: これにより、実在しない部署名を指す既存の URL の応答が「全 0」から「絞り込み無しの全件」へ変わった。アプリ内にこのパラメータを送る画面・スクリプトは無い（`Views/Analytics/Index.cshtml` と `Scripts/analytics.ts` は送らない）ので影響は外部のブックマーク・集計スクリプトに限られるが、**そういう利用側がある場合は `departmentFilterIgnored` を見て「1 部署の件数」として扱わないよう改修が要る**。
- `HomeController.Index` はダッシュボード。`period`（`week`|`month`|`quarter`|`year`、既定 `year`）で KPI・トレンド・**再発アラート**（同部署＋同種別＋原因カテゴリ重複、直近 90 日。90 日窓は `period` から独立）を計算。
- 成功/警告トーストは `TempData["Success"]` / `TempData["Warning"]`。

### ビュー / フロント

- Razor ビューはコントローラ名に対応（`src/IncidentInsight.Web/Views/`）。重症度/状態バッジは Model の計算 `*Color` プロパティ（Bootstrap カラー名）を使い、View に直書きしない。
- Bootstrap 5 / Bootstrap Icons / jQuery Validation Unobtrusive / Chart.js 4 は CDN から SRI 付きで読み込む（バンドラは無し）。
- **アプリ固有の JS は TypeScript で書く。** `src/IncidentInsight.Web/Scripts/**/*.ts` を `tsc`（`tsconfig.json`, `strict: true`）が `wwwroot/js/` へ出力し、`.csproj` の `CompileTypeScript` ターゲットが `dotnet build` 時に実行する（`wwwroot/js/*.js` は生成物なので gitignore 済み）。型チェック単独は `npm run typecheck`。
- **ページ固有のスクリプトは inline `<script>` に書かない。** Razor からデータを渡すときは `<script type="application/json" id="...-data">` のデータ島に埋め、対応する `Scripts/<page>.ts` が IIFE で読み取る（`Home/Index.cshtml` ↔ `dashboard.ts`、`Analytics/Index.cshtml` ↔ `analytics.ts`）。inline script は tsc の型チェックを受けられず、`SecurityHeadersMiddleware` が CSP を導入する際の障害にもなる。ルート直書き（`/Analytics/...`）ではなく `Url.Action` の結果をデータ島経由で渡す（PathBase 付き配備で 404 にしないため）。
- **JS から DOM へ値を流し込むときは `textContent` を使う**（`innerHTML` でのマークアップ組み立てを避ける。§9）。表示の骨組みは Razor 側に静的に置き、JS は値だけを差し替える。フェッチ失敗時は「読み込み中」表示のまま放置せず、確定した失敗表示へ切り替える（§7）。

### テスト（`tests/IncidentInsight.Tests/`）

- xUnit / `net8.0`。`Microsoft.EntityFrameworkCore.InMemory` をテストごとに `Guid.NewGuid()` 名で使う。`TempData` を使うコントローラには `TestTempData` を注入。**Mock より `InMemory` を優先**し、新エンティティを読む/書くロジックを足したら対応 `*ControllerTests` の InMemory セットアップを拡張する。`UserContextHelper.AttachUser()` でユーザーコンテキストを設定。
- **`IClock` を注入するコントローラのテストは、テストデータの日時も同じ時刻源から取る** — 本体は `IClock`（JST）で集計窓や「期限超過」を決めるのに、テストデータだけ `DateTime.Now` / `DateTime.Today`（**OS のローカル時刻**）で作ると時刻源が 2 つに割れる。`TZ=UTC` の CI ランナーでは両者が最大 9 時間ずれるため、**月末の 15:00〜24:00 UTC だけ**「本体は翌月を最終バケットにするのに、データは前月に入る」形で落ちる（issue #199。`AnalyticsControllerTests` が実際にこれで、月に 1 回・9 時間の窓でしか再現しない）。共有の仕方は 2 通りある: **既定は実行時刻に依存しない `TestFixtures.Clock`**（`FixedClock(TestFixtures.Today)` の共有インスタンス）を注入する形、**実時刻でなければ意味を持たない検査だけ** `SystemClock` のインスタンスをコントローラとデータ生成で使い回す形。**どちらを使っているテストクラスかをここに書き並べない**（参照が増えるたびに一覧だけが古くなる。`TestFixtures.Today` のコメントから同じ理由で一覧を外したのと同じ）。**この規約に検出網は無い**（意図的）— 「テストデータの日時」と「固定日を組み立てる正当な書き方・コメント中の言及」を署名から見分ける手掛かりが無く、`DateTime.Now` / `DateTime.Today` の一律禁止は、時刻源を注入していないテスト（純粋関数の検査や、コメント中の言及）まで巻き込んで**直しようの無い要求**を出す。実行不能な指示を出す検出網はいずれ緩められるため、ここは規約とレビューで守る。

### 注意点（固有の不変条件）

- **マイグレーションは起動時 `Database.Migrate()` で自動適用** — モデル変更は同一変更セットでマイグレーションを追加する。
- **ロックファイルは全プロジェクト分を同時に再生成する** — `packages.lock.json` は `src/IncidentInsight.Web` と `tests/IncidentInsight.Tests` の 2 つある。テストプロジェクトは web を `ProjectReference` で参照しているため、**web の依存が動くとテスト側の解決結果も動く**。片方だけ更新すると CI の `dotnet restore --locked-mode` が `NU1004: The project references incidentinsight.web whose dependencies has changed` で落ちる。**とくに Dependabot の nuget PR は `src/` 側しか更新しないので、そのままではマージできない**（実例: PR #180 → #181 で手当て）。取り込むときは `dotnet restore` を流して 2 ファイルとも再生成し、同一コミットに含める。ロックファイルを持つプロジェクトを増やしたときも同じ（`RestorePackagesWithLockFile` を宣言し忘れたプロジェクトは locked-mode restore も `EfCorePackageAlignmentTests` の検査もすり抜けるため、同テストが宣言漏れ自体を検出する）。
- **本番プロジェクトが直接参照する NuGet パッケージは理由付きの表と突き合わせる** — Dependabot の nuget updater は、ロックファイルを持つプロジェクトの**推移依存を更新するときその依存を直接参照へ昇格させる**。実際 PR #267 が、テスト専用の解析パッケージを `src/IncidentInsight.Web` の直接参照として足していた。**この形は版衝突が起きたときだけ赤くなる**ので（あの PR は偶然 NU1107 になって Docker ジョブだけが落ち、`build-and-test` は緑だった）、`ProductionDirectDependencyTests` が `packages.lock.json` の `type: "Direct"` を理由付きの表と突き合わせ、衝突の有無とは無関係に昇格そのものを落とす。**この形の Dependabot PR は本番側の追加を取り消し、宣言を持つプロジェクトだけを上げる。**表のキーには**プロジェクトを相対パスで**含める（ID だけだと 2 つ目の本番プロジェクトに 1 つ目の承認が黙って効き、単純名だと同名の csproj が同じバケットへ畳まれる。issue #224 / #227 と同じ形）。**この表は人が判断するエスケープハッチで、エントリが増える差分は出所・メンテ状況・本番に入れる必要性をレビューで必ず確認する**（`LengthGovernanceExclusions` と同じ扱い）。**検査の内容・対象プロジェクトの導出・残っている境界（とくに「資産が publish 出力へ入るか」は見ていないこと）は同テストの docstring が正本で、ここに書き写さない**（検査を足すたびにこちらだけが古くなる。issue #273 で実際に起きた形）。ロックファイルの書式を表すキー名と、「フレームワークごとの入れ子を降りて `type` / `resolved` を見る」歩き方は `Helpers/ProjectLockFile.cs` が唯一の参照元（読み手ごとに書き写すと、Central Package Management で `type` に `CentralTransitive` が増えたときに片方だけが直り、もう片方はその項目を黙って読み飛ばす）。**対象プロジェクトはソリューションに登録された非テストプロジェクトから導出し、読み取りは `Helpers/SolutionLayout.cs` が唯一の参照元**（CI が restore / build / test する範囲そのもの。**リポジトリ全体を `*.csproj` で走査してはいけない** —— `node_modules` やベンダーディレクトリの第三者の csproj まで拾って直しようの無い赤になり、除外表で塞ぐと今度は「登録するだけで黙らせられる口」になる。`EveryProject_HasCommittedLockFile` が同じ理由を先に記録している）。**導出が狭まっていないかは、導出とは別の手がかりで 2 方向から照合する** —— ソリューションへの登録漏れはファイルシステム（`src` / `tests` 配下の csproj）で、分類の取り違えは置き場所の慣習で見る（`IsTestProject` を手がかりにすると導出と照合が同じ判定になり、分類を取り違えたプロジェクトが両方から同時に外れて「取りこぼしゼロ＝緑」になる）。**ただし「あるパッケージが宣言している依存の版」を読む走査（`EfCorePackageAlignmentTests.ReadDeclaredDependencyVersion`）は別の問いに答えるので、そこは自分で入れ子を降り、内側のキーも別の定数（`PackageDependenciesKey`）で持つ** —— 綴りは今のところ共有側と同じだが指しているものが違う（共有側は「ターゲットフレームワークごとの解決結果」、こちらは「パッケージ内の依存一覧」）。同じ定数を使い回すと、**片方だけが改名されたときに一緒に動いてしまい**、こちらの走査が何も見つけられないまま「パッケージ名が変わった可能性があります」という誤った原因を名指しする。書式が変わったときは両方を見る。
- **`HasAtLeastOneValidMeasure` をバイパスしない**（インシデントは予防策が最低 1 件ないと登録不可）。
- **重症度/部署/インシデント種別の enum は `Incident` クラス上**（DB ではない）。値追加は static 辞書/配列＋それを回す View を更新（マイグレーション不要）。enum の日本語ラベルと Bootstrap カラーは `Models/Enums/EnumLabels.cs` に集約し、`EnumLabels.Japanese()` と `EnumLabels.Color()` の両方を更新。`IncidentTypeMapping.cs` が日本語↔DB 文字列の双方向変換を担う。
- **再発ロジックは `IRecurrenceService`**（`Services/RecurrenceService.cs`）に集約。`HomeController.Index`（90 日窓 `FindRecurrenceAlertsAsync`）と `IncidentsController.Details`（時間無制限 `FindRecurrencesForIncidentAsync`）が委譲。マッチャ `RecurrenceDetector.FindSimilar` は純粋関数。ルール変更時はサービスとそのテストを更新。
- **監査ログの正しさは `SaveChanges` 経由が前提** — `Incident` / `CauseAnalysis` / `PreventiveMeasure` に `ExecuteUpdate` / `ExecuteDelete` を使わない（変更追跡を迂回し監査漏れになる）。
- **PHI を `AuditLog.ChangesJson` に平文で残さない** — 自由記述・個人名カラムには `[Sensitive(Mask.Redact)]` か `[Sensitive(Mask.Hash)]`（`Models/Auditing/SensitiveAttribute.cs`）を付与。インターセプタが `[REDACTED]` か HMAC-SHA256 擬似匿名化（鍵 = `Audit:HashSalt`）に置換。**新しい自由記述/個人名カラムは必ず annotate**。本番で `Audit:HashSalt` 空は起動失敗。salt ローテーションは過去ハッシュとの相関を失うので runbook に記録。
  - **付け忘れは「無印を許さない」形で機械的に検出する** — `SerializeChanges` は `[Sensitive]` が**無い**プロパティの値をそのまま `ChangesJson` へ書くため、annotate を忘れた列は**ビルドも全テストも緑のまま**平文の PHI を監査テーブルへ流し込む（`AuditLog` は追記専用でインターセプタが唯一の書き込み源なので、後から気付いても書かれた行は消せない）。無印が「安全だと判断した」と「判断し忘れた」の両方を意味する限り付け忘れは検出できないので、**監査対象 3 集約の永続化される `string` 列は `[Sensitive(...)]` か `[NotPhi("理由")]`（`Models/Auditing/NotPhiAttribute.cs`）のどちらかを必ず持つ**ことを `AuditedEntityPhiClassificationTests` が固定する（分類漏れ／両方付与／**shadow property** の 3 つを検出）。検査は CLR のリフレクションではなく **EF Core のモデル（`DbContext.Model`）を読む** — インターセプタが走査するのは `EntityEntry.Properties` なので、計算プロパティ（`SeverityLabel` 等）を誤検出せず shadow property も取りこぼさない。`[NotPhi]` は理由が必須（空文字・空白は実行時例外）で、「とりあえず検出網を黙らせる」使い方を塞いでいる。現在の除外は 5 件で、いずれも閉じた語彙（`Incident.Department` / `Incident.Severity` / `Incident.IncidentType` / `PreventiveMeasure.Status` / `PreventiveMeasure.MeasureType`）。
  - **「string 列」の判定は「CLR の型が `string`」と「DB へ文字列として保存される」の和集合** — どちらか一方への置き換えにすると、外れた側が誰にも見られない列になる。**本命は CLR 側**で、`SerializeChanges` が `ChangesJson` へ書くのは `prop.CurrentValue` / `OriginalValue`＝**変換前の CLR の値**だから（自由記述列に暗号化の値変換 `string → byte[]` を足すと「文字列として保存されない」列になるが、監査ログへ流れるのは相変わらず平文の `string`）。一方で `HasConversion` を通す enum 列は CLR が enum のままなので、CLR 側だけでも足りない。しかも**変換後の型が現れる場所は書き方によって違う**（実測）: `HasConversion<string>()` は `GetProviderClrType()` に出て `GetValueConverter()` は `null`（`Severity` / `Status` / `MeasureType`）、`HasConversion(v => …, v => …)` は逆（`IncidentType`）。`AuditedEntityModel.IsStringColumn` がこの 3 つすべてを見る。
  - **shadow property（CLR プロパティを持たない列）は禁止する** — `LookupSensitiveMask` は CLR プロパティに付いた `[Sensitive]` を読むため、shadow の `string` 列は**原理的にマスクできず必ず平文で** `ChangesJson` へ書かれる。分類漏れの対処法は「属性を付ける」だが shadow の対処法は「CLR プロパティへ昇格させる」で別物なので、専用の検査（`PersistedStringColumns_MustHaveBackingClrProperty`）が固有のメッセージで落とし、他の 2 つの検査は shadow 列を対象から外す。実行不能な指示を出す検出網は、いずれ「直せないので検査を緩める」方向へ倒れるため。
  - **長さ上限の検査対象は「`[Sensitive]` 付きの列」ではなく永続化される文字列列すべて** — `FreeTextMaxLengthAttributeTests` が上限の付け漏れを落とす。以前は `[Sensitive]` 付きだけを見ていたが、**分類の選択肢に `[NotPhi]` が増えた時点でそれが穴になった**（`[NotPhi("...")]` だけ付けて `[MaxLength]` を書き忘れると両テストとも緑のまま無制限の列が通り、しかも `[NotPhi]` 列は定義上マスクされないので無制限の値が監査ログへ平文で積まれる）。**エスケープハッチを足すと既存の検出網がその分だけ黙って狭くなる**ので、条件は分類の種類に依存させず「列であること」に置く。上限の判定も `[MaxLength]` 属性ではなく **EF のモデルの `GetMaxLength()`** を読む（DB の列長を決めているのはモデル側の値で、fluent の `HasMaxLength()` で設定した列を「上限なし」と誤判定しないため）。
  - **監査対象の一覧を読む側は必ず `AuditSaveChangesInterceptor.AuditedEntities` から導出する** — 現在の導出先は `AuditLogsController.AllowedEntityNames`（フィルタ許可リスト＋ドロップダウン）と 2 つのテスト（`AuditedEntityPhiClassificationTests` / `AuditEntityLabelCoverageTests`）。**長さ上限まわりの検査は監査対象から導出しない**（下記）。写しを持つと、監査対象を足したときにインターセプタだけが増えて写し側が取り残される。とくに `AllowedEntityNames` の取り残しは **fail-open**（`Contains` が false になって絞り込みが黙って外れ全件返る）＋**画面から到達不能**（ドロップダウンに出ない）の二重のずれを同時に起こす。`AuditedEntities` は集合ではなく**順序付きの `IReadOnlyList`** で、並びはドメインの順（ルート集約のインシデントが先）＝そのままドロップダウンの表示順。導出先で並べ替えない（型名の序数順にすると、日本語 UI の並びが英語の識別子の綴り順という無意味な順になる）。順序を持たせた代わりに重複が書けてしまうので、`AuditedEntities_HasNoDuplicates` が固定する。
  - **導出できない写しが 1 つだけ残っている: `EnumLabels.AuditEntityJa`（日本語ラベルの変換表）** — `JapaneseAuditEntity` は辞書に無いキーを**元の値のまま返す**フォールバックを持つため、ラベルを書き忘れても例外にならず、監査ログ画面の 3 箇所（ドロップダウン／一覧の各行／詳細）に CLR の型名が英語のまま出る。フォールバック自体は残す（監査対象から外したエンティティの過去行を表示するとき、例外で画面を落とすより安全）ので、代わりに `AuditEntityLabelCoverageTests` が「ラベル表は監査対象を全網羅する」ことを固定する。
  - **長さ上限の値は `[MaxLength]` 属性でも fluent の `HasMaxLength()` でも `FieldLengths` の定数を使う** — 上限の**充足**は EF のモデルで判定するので fluent でも通るが、そのぶん fluent が「裸の数値の抜け道」になる（上限はある＝緑、でもその値は誰も見ていない）。`FieldLengthsTests` は属性側（`EveryMaxLength_UsesAFieldLengthsConstant`）とモデル側（`EveryModelMaxLength_UsesAFieldLengthsConstant`）の**両方**を見る。許容値は**意図的に別集合**にしてある: モデル側は `{FreeText, ShortText, EnumCode, EnumCodeJapanese}`、属性側は**「値変換して文字列として保存される enum 列」だけ**にモデル側と同じ集合を許し、それ以外は `{FreeText, ShortText}`。判定は宣言型ではなく**そのプロパティ自身が文字列として保存される列か**で行う（宣言型で見ると、同じエンティティにある `[NotMapped]` の enum や既定の int マッピングのままの enum 列にも緩和が届く。型が enum かだけで見ると ViewModel の enum プロパティにも届き、`MaxLengthAttribute` は enum 値に対して実行時に `InvalidCastException` を投げるのでその画面の POST が毎回 HTTP 500 になる）。`{EnumCode, EnumCodeJapanese}` は「値変換して文字列として保存する enum 列」にしか意味を持たないので、許す範囲もそこに限る。
  - **扱う長さの単位は「文字数」だけで、それ以外は値を見る前に落とす** — このリポジトリに `byte[]` のバイト長やコレクションの件数を制限する項目は**1 つも無い**。単位ごとの定数と日本語メッセージ書式を先回りで用意すると、実在しない事情のために分類・許容値・文言の対応表が増え、その分岐そのものが欠陥を生む（§6 の「将来を見越した過度な抽象化を避ける」）。そこで単位を文字数に限り、`LengthLimit_OnlyOnCharacterProperties`（属性側）と `LengthLimit_OnlyOnCharacterColumns`（fluent 側）が**文字数を数えないところに付いた長さ上限を落とす**。対象は「`string`」と「値変換して文字列として保存する enum 列」の 2 つだけ（`FieldLengthsTests.IsCharacterLengthProperty` / `AuditedEntityModel.IsStringColumn`）。この 1 つの規則が 2 つの事故を同時に塞ぐ: **(a) 測れない型**（`enum` / `int` / `DateTime`）に長さ属性を付けると `MaxLengthAttribute.IsValid` が `InvalidCastException` を投げてその画面の POST が毎回 HTTP 500 になるのに、値も文言も規約どおりなら他の検査は全件緑になる（実測）。**(b) 単位またぎ** — バイト長や件数を文字数用の定数（`FreeText`=500）と文言（「◯◯は500**文字**以内で…」）で表してしまう。fluent 側を別に持つのは、属性を書かずに `HasMaxLength()` だけで同じことができるため（実測でも `byte[]` 列への `HasMaxLength(300)` は当時の長さ関連の検査すべてを素通りした）。**別の単位の上限を最初に足す人が規則を拡張する**（用途を表す定数と単位に合った書式を `FieldLengths` へ追加し、分類を単位ごとへ広げる）。検査の失敗メッセージがその手順を案内する。
  - **属性側の走査対象を「`string` の CLR 型」だけに絞ってもいけない** — 3 つの実測がある。(1) 値変換した enum 列に裸の `[MaxLength(200)]` を書くと、モデル側の検査は許容値のどれかに当たれば通り、属性とモデルの一致検査も（値が同じなので）通って**全件緑**になる（だから上の「文字数を数えるもの」に enum 列を含めてある）。(2) **型を選ぶ側**まで `string` 依存にすると、長さ属性が非 `string` プロパティにしか付いていない型が `[Theory]` のケースにすら入らず、テスト件数すら変わらないまま素通りする（`GovernedTypes` は属性の有無だけで選ぶ）。(3) モデル側の裸の数値の検査を文字列列に絞ると、fluent で非文字列列へ設定した上限が誰にも見られなくなる。**「検査する範囲」と「対象を選ぶ範囲」は必ずそろえる**（差分がそのまま死角になる）。
  - **残っている境界: 入力型には「上限の付け忘れ」の検査が無い** — エンティティ側には `PersistedStringColumns_MustHaveMaxLength`（上限の付け忘れを落とす）があるが、属性側の対象は「長さ上限の属性を既に宣言している型」から導出するので、**上限をまったく書いていない入力欄は最初から視界に入らない**。この非対称は意図的で、機械的に閉じられない: 実測すると EF のモデルに載らない自前の `string` プロパティは 28 件あり、その大半（`AuditLogListViewModel` のフィルタ表示、`DashboardViewModel.Period`、`MonthlyCount.Label` のような表示専用、`AuditOptions.HashSalt` / `NotPhiAttribute.Reason` / `PendingAudit.*` のような内部の配管）は**上限が無くて正しい**。「画面へバインドされる入力欄か」を見分ける確かな目印が無いため、一律に上限を要求すると実行不能な指示を大量に出す（この repo が繰り返し避けている形）。**入力用 ViewModel に自由記述の欄を足すときは、対応するエンティティ列と同じ `FieldLengths` の定数を手で付ける**（付け忘れると、画面は素通りするのに SQL Server / PostgreSQL で列長超過＝HTTP 500 になる）。
  - **残っている境界: 「誰が長さを設定したか」は EF のモデルから分からない** — 列単位の絞り込みは「その CLR プロパティを誰が宣言したか」で切っているので、`ApplicationUser.Email`（Identity が宣言）に対して**自分たちの `OnModelCreating` で** `HasMaxLength(37)` と書くと、どの検査も拾わない（実測。スナップショットには残り、SQL Server / PostgreSQL 向けに `Migrations/` を再生成すると `nvarchar(37)` になる）。EF のモデルは長さの設定元を区別できないため機械化できない。**基底クラスが宣言した列に対して fluent で長さを設定する差分は、レビューで理由を確認する**（除外表と同じ扱い）。モデル側の対象は `AuditedEntityModel.LengthGovernedEntityTypes()`＝**「自分たちのアセンブリで定義されたマップ済みエンティティ」から `AuditLog`（監査証跡スキーマ固有の列長）を除いたもの**で、判定は CLR プロパティの有無を問わず（shadow 列も含めて）**自分たちが宣言した**文字列列すべてに掛ける。基底クラスが宣言した列（`ApplicationUser` が継承する `UserName` / `Email` など、列長を Identity が決めるもの）は対象外 — この列単位の切り分けは下の項を参照。属性側の対象は型名を書き並べず、**「自分たちのアセンブリで長さ上限の属性を 1 つでも宣言している型」**から導出する（エンティティも ViewModel も同じ条件で拾う。名前空間や `"ViewModel"` という接尾辞に依存しないので、置き場所や命名を変えても対象から外れない）。
  - **「長さ上限の属性」は `[MaxLength]` だけではない** — `[StringLength]` と .NET 8 の `[Length(min, max)]` も MVC の入力検証（`[StringLength]` は EF の列長も）が尊重するので、`[MaxLength]` だけを見ると**綴りを変えるだけの抜け道**になる。実測でも ViewModel に `[StringLength(200)]` / `[Length(1, 200)]` を書くと全件緑のまま通り、裸の数値も `ErrorMessage` の指定漏れ（＝日本語 UI に英語の既定メッセージ）も素通りした。しかも後者はテスト件数すら変わらないので痕跡がゼロ。**属性の解釈は `AuditedEntityModel.ReadLengthLimits` の 1 か所に集約し、DataAnnotations に長さ上限の属性が増えたらそこへ足す**（名前空間・型名の接尾辞への依存をやめたのと同じ理由で、属性名への依存も残さない）。属性は `inherit: true` で読む（基底で宣言し派生で `override` した列を「属性なし」と誤判定すると、fluent の `HasMaxLength()` との食い違いが黙って素通りする）。
  - **長さ上限の管理範囲を監査対象から導出しない** — 「どのエンティティを監査するか」と「どのエンティティの列長を管理するか」は別の関心事で、前者から後者を導くと**監査ポリシーの変更が無関係な長さ管理まで黙って外す**（あるエンティティを監査対象から外した瞬間、裸の `[MaxLength(200)]` も上限の付け忘れも値変換列の切り詰めもまとめて素通りになる。すべて fail-open）。アセンブリベースで導出するので新しいエンティティは何もしなくても検査対象に入る。実際この分離で `CauseCategory.Description` が**上限なしのまま残っていた**のを検出し、`FieldLengths.FreeText` を付けた（同一変更セットでマイグレーション追加。SQLite は TEXT に長さ制約が無いため本体は空で、スナップショットへの記録が目的）。
  - **導出の条件は「名前空間の完全一致」ではなく「所属アセンブリ」にする** — 長さ上限の検査は複数ある（裸の数値の禁止・上限の付け忘れ・値変換列の切り詰め・単位の限定・属性とモデルの一致・その網羅ガード。**件数を数えて書かない** —— 検査を足すたびにこの数字だけが古くなる）が、対象範囲は `LengthGovernedEntityTypes()` の 1 か所が決めている。**この導出が 1 つでもエンティティを取りこぼすと、そのエンティティはそれらすべてから同時に、しかも黙って外れる**（すべて fail-open）。以前の `t.Namespace == "IncidentInsight.Web.Models"` は文字列の完全一致で、この repo は既に `Models/Enums` / `Models/Auditing` / `Models/Validation` 等のサブフォルダを持つため、**エンティティを 1 つサブフォルダへ移すだけ**で一致しなくなる。実測でも「導出集合から外れ、同時にその列の `[MaxLength]` が消える」変異が**全件緑のまま通った**（唯一の痕跡はテスト件数が 496 → 490 に減ることだけで、正当なリファクタと見分けが付かない）。アセンブリ単位なら名前空間をどう切り直しても外れない。**エンティティ単位の除外は `AuditedEntityModel.LengthGovernanceExclusions`（理由付きの表）だけ**で、現在の登録は `AuditLog` の 1 件。この表が意図的な除外の唯一の真実の源で、導出・網羅ガード・属性側の対象導出が同じここを読む（写しを持つと、どちらへ除外を足しても片方が取り残される。実測でも、ガード側の表にだけ登録した型はモデル側 3 検査から除外されず、「除外したはずの型」で検査が落ちた）。**`ApplicationUser` を `IdentityUser` 派生として型ごと除外してはいけない**（下記の列単位の項を参照）。
  - **除外は「エンティティ単位」ではなく「列単位」で切る** — `ApplicationUser` を型ごと外すと、`DisplayName`（個人名）と `Department`（`Incident.Department` と同じ語彙）まで巻き添えで長さ管理から永久に外れる（実際この 2 列は上限なし＝SQL Server なら `nvarchar(max)`、PostgreSQL なら `text` のまま残っていた）。列長を Identity が決めるのは **Identity 自身が宣言した列**（`UserName` / `Email` / `PasswordHash` など）だけなので、`AuditedEntityModel.IsDeclaredInOwnAssembly`（宣言元の型が自アセンブリか）で列単位に切る。判定を 1 か所に置くのは、この絞り込みを使う検査が複数あり、各検査が `DeclaringType?.Assembly == …` を書き写すと条件を直したときに片方だけ取り残されるため。shadow 列は宣言元をたどれないが自分たちのモデル由来なので**残す**。
  - **上限の付け忘れを見る検査も shadow 列を対象にする** — 以前は `ClrBackedStringColumns` に絞り「shadow は `AuditedEntityPhiClassificationTests` が専用の対処法で落とす」としていたが、その検査の対象は**監査対象 3 集約だけ**。監査対象でない `CauseCategory` へ上限なしの shadow 列（`Property<string>("...")`）を足すと、長さ関連の各検査すべてを素通りした（実測で全件緑）。上限の検査は属性を読まないので shadow 列も対象にできる。
  - **導出が効いているかは独立な手がかりで照合する** — `FieldLengthsTests.LengthGovernedTypes_CoverEveryOwnedDbSet` が、導出とは別の宣言箇所である **(a) `ApplicationDbContext` が自分で宣言した `DbSet<T>`**（`BindingFlags.DeclaredOnly` ＋ 自アセンブリの基底までを走査。Identity が宣言する `Users` / `Roles` を拾わず、将来 `DbSet` を自前の中間基底へ移しても追随する）と **(b) 基底の総称 DbContext へ自分たちが渡した型引数**（`IdentityDbContext<ApplicationUser>` の `ApplicationUser`）の両方と突き合わせ、管理対象でも「意図的な除外」でもないエンティティがあれば落とす。**(b) が要る**のは、`ApplicationUser` が Identity 側の `DbSet`（`Users`）で公開されるため (a) の `DeclaredOnly` に掛からないから — (a) だけだと、導出へ `IdentityUser` 派生の除外を戻すと `ApplicationUser` が長さ関連の検査から同時に消えるのに全件緑のまま通った（実測 504 → 500。痕跡はテスト件数の減少だけ）。同じ経路でガードを書くと導出が狭まったときにガードも一緒に狭まり「取りこぼしゼロ＝緑」で無力化されるため、**手がかりを変える**のが要点。`DbSet` を 1 つも読めない場合も「見るべき対象ゼロ＝緑」を避けるため fail-closed で落とす。意図的な除外は `AuditedEntityModel.LengthGovernanceExclusions`（**完全修飾名をキーにした理由付きの表**。単純名だと将来同じ単純名のエンティティを足したときに巻き添えで外れる）に登録する。この表には 3 つの検査が掛かる: `LengthGovernanceExclusions_AreAllStillReal`（キーが EF のモデル上に実在する）/ `LengthGovernanceExclusions_AllHaveAReason`（理由が空・空白でない。値を誰も読んでいなかったため、理由を `"   "` にすれば長さ関連の検査すべてを黙らせられた）/ `LengthGovernanceExclusions_CannotDropIdentityBackedEntities`（手がかり (b) の型＝`ApplicationUser` は載せられない。もっともらしい理由で登録して `[MaxLength]` を消すと全件緑のまま通ったため）。
  - **残っている境界: 除外表そのものは人が判断するエスケープハッチ** — `AuditLog` を除外してよい理由は「列長の出所が `FieldLengths` ではない」ことだが、これを機械判定に落とすことはできない。「除外した型は全列に上限がある」を条件にする案は `AuditLog.ChangesJson` が**意図的に無制限**（JSON 本文なので `nvarchar(max)` / TEXT 相当）なので成立しない。したがって、DbSet で公開している業務エンティティ（`Incident` / `CauseAnalysis` / `PreventiveMeasure` / `CauseCategory`）をもっともらしい理由付きで登録し、同時にその列の `[MaxLength]` を消すと、長さ関連の検査すべてから外れて**全件緑のまま通る**（実測。痕跡はテスト件数の減少だけ）。`Microsoft.Data.SqlClient` のピンと同じく**期待値を機械化しない代わりにレビューで見る**箇所なので、**この表にエントリが増える差分は、理由の妥当性をレビューで必ず確認する**。
  - **`[MaxLength]` と fluent の `HasMaxLength()` を両方書くなら値を一致させる** — fluent が優先されるため、食い違うと「画面は属性の上限で検証し、DB は fluent の上限で作られる」層またぎのずれになる。上限の**充足**をモデルで判定する以上どちらも「上限あり」で緑になるので、一致そのものを `ModelMaxLength_AgreesWithMaxLengthAttribute` が固定する。
  - **値変換で文字列として保存する列は、実際に保存しうる値がすべて収まる長さにする** — 上の検査が裸の数値を禁じることで `FieldLengths.EnumCode` の使用を積極的に誘導するため、定数が実際の値より短いと誘導に従った結果として切り詰めが起きる。壊れ方は**プロバイダ依存**（SQL Server / PostgreSQL は例外、SQLite は黙って保存、テストの InMemory は列長の概念すら無い）で、**ビルドも全テストも緑のまま特定の配備先でだけ壊れる**。`ConvertedEnumColumnLengthTests` が、変換器を実際に通した文字列（`HasConversion<string>()` なら enum 名、ラムダ変換なら変換結果）の長さを上限と突き合わせて固定する。**検査範囲は誘導する検査と同じ**にする（誘導する範囲より検証する範囲が狭いと、その差分がそのまま死角になる）。あわせて `EveryLengthLimitedEnumColumn_IsActuallyExamined` が「見るべき列を全部見たか」を**判定とは独立な手がかり**（enum 型でかつ長さ上限を持つ永続化列）で照合する — 同じ判定でガードを書くと、判定が狭まったときにガードも一緒に狭まって「違反ゼロ＝緑」で無力化されるため。文字列として保存する enum 列の上限は `FieldLengths.EnumCode`（20）と `FieldLengths.EnumCodeJapanese`（50。`IncidentType` は日本語の DB 文字列を保存するため）。
- **キャッシュ抑止は「誰も書かなければ `no-store`」の 1 規則で、指示を書く側が 3 つある** — `SecurityHeadersMiddleware` が応答開始直前に、キャッシュ指示の**無い**応答へ `Cache-Control: no-store` を入れる（応答の種類では振り分けない。CSV・添付画像のエクスポートを足しても既定で保護される）。**既に指示がある応答には一切触れない**のが要点で、それが静的アセットを `public,max-age=3600` のまま残す唯一の仕組み — `Program.cs` の `UseStaticFiles(OnPrepareResponse)` が自分で名乗ることで既定の対象から外れる。**この 2 ファイルは対になっており、片方だけを動かすと壊れる**（`OnPrepareResponse` を消すと css/js が毎回再取得になり、ミドルウェアの「譲る」挙動を消すと今度は静的アセットが `no-store` になる）。
  - **ミドルウェアより手前で応答が完結する経路が 2 つあり、そこには `no-store` が付かない**（`docs/security.md` の「例外」が正本）。本番の `UseHttpsRedirection`（307・本文なし）と、`AllowedHosts` を実ホスト名へ絞ったときのホスト名検査（400）。後者は汎用ホストが `IStartupFilter` として登録するので **`Program.cs` の並べ替えでは手前に出られない**。実害が無い理由は「本文が空だから」**ではなく**（実測: 400 には `Bad Request - Invalid Hostname` の定型ページが返る）、「定型文で、要求元のホスト名も業務データも含まないから」。その 2 点は `HostFilteringShortCircuitTests` が固定する。**この行が `UseStaticFiles` より前にいることは、配信された静的資産の応答にセキュリティヘッダーが付くかで固定してある**（`StaticAsset_StillGetsTheSecurityHeaders`。issue #257） — `UseStaticFiles` は一致したファイルに対して終端なので、後ろへ動かすと `wwwroot` 配下のすべての資産が `X-Content-Type-Options` / `X-Frame-Options` / `Referrer-Policy` を一斉に失う。以前は守るものが何も無く、実測で全件緑のまま通った。**行の位置ではなく応答を見る**（ソースの並びを見る検査は書き方に弱い）。また、**`app.UseMiddleware<SecurityHeadersMiddleware>()` の行を「本番ブロックの中へ移す」直し方をしない** — そのブロックは `if (!IsDevelopment())` の中なので、移すと Development でヘッダーもキャッシュ抑止も丸ごと消える。
  - **譲る相手の中身は問わないので、指示を書く側を全部塞ぐ必要がある。** PHI を返すアクションが「保存してよい」と名乗った瞬間、ミドルウェアは黙って譲る。書ける場所は 3 つあり、それぞれ別の検査が担当する:
    1. **`[ResponseCache]` の宣言** — `NoStore = true` が無い宣言は `ResponseCacheAttributePolicyTests` が落とす（`CacheProfileName` を使う形も、実際の指示が属性から読めないので fail-closed で落とす）。**「宣言」には<b>フィルタの間接指定</b>も含める**（issue #281）—— `[TypeFilter(typeof(LongCacheAttribute))]` / `[ServiceFilter(typeof(...))]` で被せると、付いている属性は `TypeFilterAttribute` なので `a is ResponseCacheAttribute` で照合する述語には一致せず、**3 つの検査がそろって素通りした**（属性名に `Cache-Control` の綴りが無いのでソース走査でも拾えない。実測で PHI を返すアクションへ `public,max-age=300` を付けても全件緑）。述語は `ResponseCachePolicy.IsResponseCacheDirective`、判定は `JudgeDirective` の 1 組に寄せ、**間接指定は中身を読めないので fail-closed で落とす**（`CacheProfileName` を落としているのとまったく同じ理由 —— 同じ理由の判断を 2 つ違う向きにしない）。**間接指定を辿る入り口は `EffectiveFilterTypes` 1 つ**にして応答キャッシュ側と出力キャッシュ側が共有する（別々に書くと片方だけが辿る状態が生まれる）。**残る境界は `IndirectFilterTarget` の docstring が正本**（自前の `IFilterFactory` と入れ子の間接指定は宣言から型が見えないので追えない。**ここに書き写さない**）。**出力キャッシュ側の型名照合は単純名で行い、`Split('.').Last()` に戻さない** —— `FullName` の綴りは形で変わるため（入れ子は `+` 区切り、総称は `` `1 `` が付く）、最後のドット区切りでは**素の型でしか**一致せず、しかもプローブ側の照合は素の型で通るので狭いままでも緑になる。規則は `SimpleTypeNameOf` が正本で、`SimpleTypeNameOf_DropsNamespaceNestingAndArity` が合成した綴りで固定する。**走査の入り口は 1 つだけにし、狭い述語を抱えた型付きの入り口を別に持たない**（レビュー指摘・実測）—— 以前は `[ResponseCache]` 専用の `DeclarationsOn` があり、アプリ全体の検査を**そちらへ向け直すだけ**で間接指定の手当てがまるごと外れ、全件緑のまま通った（「より型が付いているほうへ寄せる」のは DRY の整理として自然に見えるので差分からも読み取れない）。**述語そのものを固定する検査とは別に、呼び出し側が渡す述語にも空振り検出を置く**（`AssertThePredicateFollowsFilterIndirection`。述語を直接呼ぶテストは、本番の呼び出しが別の述語へ差し替わっても気付かない —— `ownAssembly` とホスト集合に空振り検出を置いているのとまったく同じ理由）。出力キャッシュ側は同じことを**プローブに間接指定を混ぜる**ことで見る。対象は**アセンブリ上の具象型すべて**から導出する（`AppControllerScan.CacheDirectiveHosts`）。**基底型で絞らない**のが要点で、`[ResponseCache]` は `ControllerBase` 専用ではなく `PageModel`（Razor Pages）でも同じように効くため、`ControllerBase` で絞ると `Pages/Export.cshtml.cs` に付けた宣言が**どの検査からも見えなくなる**（属性名にはヘッダー名の綴りが無いので、下の 3. のソース走査でも拾えない）。絞らないことの代償は無い（その属性が付いた宣言しか拾わないので誤検知は増えない）。狭め直す変異は `CacheDirectiveHosts_CoverEveryConcreteTypeInTheAssembly` が落とす（**「非コントローラの型を 1 つでも含む」では足りない** — 実測で `ControllerBase || Namespace.Contains("Models")` へ狭めると ViewModel が条件を満たして全件緑のまま `Pages/` の宣言が再び見えなくなった。だから「1 つも欠けていない」を条件にし、テスト自身がアセンブリを数え直して突き合わせる）。あわせて**抽象基底へ引き上げたアクション**も拾う。**拾い方は「具象から継承の連なりを 1 段ずつさかのぼり、各段を `inherit: false` で読む」**（issue #275）—— 各段はそれ自身が宣言した分だけを返すので、名指しは**実際に宣言している型**（`override` を含む）に自然となり、継承して見えた分を後から打ち消す仕組みが要らない。**具象だけを `DeclaredOnly` で読む形にはしない**（基底は抽象なので走査対象に入らず、どこからも見えなくなる）。**この「1 段ずつ訪ねる」形は、以前の「具象ごとに `inherit: true` で読んでから宣言元をたどって打ち消す」形を置き換えたもの**で、(a) 同じ宣言を複数の派生から見る重複が構造的に起こらない（畳むのは宣言の内容ではなく**訪ねた段**で、閉じた総称は開いた定義へそろえてから数える）、(b) **同じ段に複数付いた宣言はそのまま件数として返る**ため、2 個目が消えることを検知する fail-closed の門番（`EnsureNothingWasLost`）ごと不要になった。**報告するのはソースに書かれた宣言すべてで、実行時にその派生で効くものだけではない**（派生が同じ種類を宣言し直していても、基底の宣言は別の派生からは効くし、`override` しない派生が足された時点で効き始める。狭めると無言の fail-open になる）。**アクション側だけを自アセンブリで切り、クラス側は連なりのどの段でも読む**（フレームワーク側の型が宣言したクラス属性も、以前の `inherit: true` の読み方では見えていた）。**この「置く位置」と「飛ばし方」には専用の検出網が要る** — 絞り込みをクラス側の読み取りより前へ持ち上げる形も、`continue` を `break` へ変える形も、どちらも「同じ条件をまとめただけ」に見えるのに走査範囲が黙って狭まり、実測では全件緑のまま通った（共有プロジェクトへ切り出した基底コントローラが `[ResponseCache(Duration = 300, Location = Any)]` を持つと、正しい実装は報告するのに狭めた実装は黙って落とす）。`AttributeScan_StillReadsClassAttributes_OnSitesOutsideTheOwnAssembly` が、**`ownAssembly` 側に別のアセンブリを渡して外と内の向きを入れ替える**という手がかりで固定する（実際に別アセンブリの基底は用意できないため）。**アプリ全体を見る 2 つの検査はどちらも「違反が 0 件」しか見ないので、走査が何も返さなくなると無条件で緑になる** —— 実測でも、呼び出し側が渡す `ownAssembly` を別のアセンブリへ差し替えると全件緑のまま通り、そのうえで PHI のダッシュボードへ `[ResponseCache(Duration = 300, Location = Any)]` を足しても緑だった。**だから呼び出しごとに空振り検出を置く**（`AssertTheAppWideScanIsLive`）。**目印に「実在する宣言」を選ばない** —— 当初は唯一の `[ResponseCache]` である `HomeController.Error()` を目印にしていたが、ミドルウェアが既定で `no-store` を書く以上あの宣言は冗長で、消すのは自然な整理（他に固定している検査も無い＝実測）。消した瞬間に無関係に見える検査が 2 つ落ち、緑へ戻す道が「冗長な属性を production へ戻す」か「検査を消す」しか無くなる。代わりに **(a) 渡した `ownAssembly` がホスト集合の出どころと一致すること**と、**(b) 同じ引数へ「すべての属性に一致する述語」を通すとアクション側の宣言が返ること**を見る（アクションには `[HttpPost]` / `[Authorize]` が必ず付くので蒸発しない）。**(b) だけでは取り違えを拾えない** —— 連なりは必ず `object` まで届くので、`typeof(string).Assembly` を渡すと*その段で*アクション側の走査が走り、`object` 自身のメソッドの属性で条件が満たされて全件緑になった（実測）。**空振り検出は同じ呼び出しの引数を受け取る** —— 別の呼び出しで導出し直すと、こちらの引数だけを差し替える変異に届かない。**ホスト集合の絞りすぎは別の検査が見る**（`AssertTheHostSetIsNotNarrowedToControllers`。コントローラでない具象型が 1 つでも含まれていること。狭めると `Pages/` の宣言が再びどの検査からも見えなくなるのに、導出関数を見る `CacheDirectiveHosts_CoverEveryConcreteTypeInTheAssembly` は呼び出し側がどちらを呼んでいるかを見ないので代わりにならない）。**述語の「出力キャッシュ側」も別に確かめる** —— `[ResponseCache]` を混ぜた述語は空振り検出をそちらで満たしてしまうため、出力キャッシュ側だけを落とす「簡略化」が全件緑で通った（実測）。同じ述語を**本物の `[OutputCache]` を付けたプローブ**へ当てて固定する。**型名で照合する理由は「依存を避けるため」ではない**（実測で訂正。`Microsoft.AspNetCore.OutputCaching` は共有フレームワークの一部で、テストプロジェクトから既に参照できパッケージ追加は要らない）—— 本当の理由は**名前空間を問わず拾いたい**ことで、プローブ側は実型を参照して framework の改名をコンパイルエラーとして受け取る（型名を合成すると綴りの写しが 2 つでき、改名時にどちらも緑のまま照合だけが外れる）。**残っている境界**: アクション側を自アセンブリに絞る 1 行を**外す**変異には検出網が無い（外しても拾う属性がフレームワーク側に無く違反が増えないため。倒れる向きが過剰報告＝安全側なので許容している）。
    2. **`MvcOptions.Filters` のグローバルフィルタと `MvcOptions.CacheProfiles`** — どちらも属性のソースに現れない。`ResponseCacheHeaderIntegrationTests` が**起動したアプリの設定**から読み、**同じ判定関数**（`Helpers/ResponseCachePolicy`）に通す。**フィルタ側を型で絞り込まない**（issue #281）—— `Filters.OfType<ResponseCacheAttribute>()` で引いていたころは `Filters.Add<LongCacheAttribute>()` が**1 件も見られていなかった**（`FilterCollection` が格納するのは `TypeFilterAttribute` であって属性そのものではない）。一覧を全部見たうえで共有の述語に判定させる形（`CachingFilterViolations`）にし、**判定そのものは合成入力で固定する** —— 本番の設定に違反は 1 件も無いのが正しいので、判定を「常に空」へ潰しても全件緑のまま通る。**その合成入力は本物の `FilterCollection` へ `Add<T>()` で登録する**（手で `TypeFilterAttribute` を並べると、この検査の前提そのものを仮定してしまい、framework が格納の形を変えたときに気付けない）。
    3. **応答ヘッダーへの直接の書き込み**（`Response.Headers.CacheControl = "public,max-age=300";`）— 属性にも設定にも現れず、実行時の 1 文なのでリフレクションでは原理的に見えない。**手がかりを変えてソースの実在を見る**（`OnlyIntendedWriters_SetCacheControlDirectly`）。走査は **Web プロジェクト全体の `.cs` と `.cshtml`**で、**意図して書く 2 か所だけ**を理由付きの許可表（`IntendedCacheControlWriters`）に置く。**表は「ファイル × 行の内容 × 期待する出現回数」まで持つ** — 内容の一致だけで許すと、2 つ目の `UseStaticFiles`（`wwwroot` の外を `/attachments` として配る）が共有定数 `SecurityHeadersMiddleware.StaticAssetCacheControl` を再利用した瞬間に行が一字一句同じになり素通りする（しかも定数の再利用は §6 が積極的に要求する書き方で、既存ブロックのコピーでも自然にそうなる）。実測でも、その差分は `StaticAssets_AreOnlyApprovedPublicAssets`（`wwwroot` の外は見ない）にも掛からず**全件緑のまま** `/attachments/<id>/report.pdf` が `public,max-age=3600` で返っていた。回数まで見れば、行を複製した形は必ず「回数が合わない」として現れる。**ただし回数の照合だけでは足りない** — 2 つ目の配信ルートが `StaticFileOptions` を変数へ括り出して 2 回渡す形・`OnPrepareResponse` を代入して共有する形は、**許可済みの行を 1 本も増やさない**ので素通りする（実測で全件緑。しかも括り出しは §6 が要求する書き方そのもの）。そこで**手がかりを「指示の綴り」から「配線の綴り」へ変えた検査を対にする**: `OnlyIntendedPlacesWireUpStaticFileServing` が `UseStaticFiles` / `UseFileServer` / `UseDirectoryBrowser` / `PhysicalFileProvider` の出現を理由付きの許可表 `IntendedStaticFileWiring`（ファイル × 綴り × 回数）と突き合わせる。**見張る綴りは「配信を組み立てる呼び出し」（`UseStaticFiles` / `UseFileServer` / `UseDirectoryBrowser`）だけでは足りず、「配信の根を差し替える綴り」（`FileProvider` / `PhysicalFileProvider` / `CompositeFileProvider`）も要る** — 配信ルートは呼び出しを増やす以外に**既存の呼び出しを広げる**形でも増えるため。実測では、既存の `UseStaticFiles` に `FileProvider = new CompositeFileProvider(WebRootFileProvider, ContentRootFileProvider)` を足すと呼び出しの回数も指示の行も 1 つも変わらないまま `/appsettings.json` ・ `/incident_insight.db` ・ `/Views/**/*.cshtml` が配られ、**全件緑のまま通った**。**差し替えはパイプラインの組み立てだけでなくビルダの生成時にも起こる**ので、`WebRootPath` / `UseWebRoot` も見張る（実測: `WebApplication.CreateBuilder(new WebApplicationOptions { WebRootPath = "." })` にすると呼び出しも指示も 1 つも変わらないまま `/appsettings.json` が配られ、全件緑のまま通った）。これら 5 綴りは現時点で Web プロジェクトに 1 つも無いので、許可表に載せない＝1 つでも現れたら落ちる。**この表にエントリ・回数が増える差分は、その配信ルートが PHI を配らないことをレビューで必ず確認する。**

    **`wwwroot` 配下は「入れ物」と「ファイルの種類」の 2 つの表で見る**（`StaticAssets_AreOnlyApprovedPublicAssets`）。配信の `OnPrepareResponse` は `wwwroot` 配下の**すべて**に `public,max-age=3600` を名乗らせるので、走査も**再帰**で配下すべてに掛ける — 以前は直下しか見ておらず、**承認済みの入れ物の中に新しい入れ物を作ると素通りした**（`wwwroot/js/exports/patient-report.pdf` は直下のエントリを 1 つも増やさないので全件緑のままだった。issue #254）。入れ物を増やさずファイルだけ置く形も同じ理由で素通りするため、ファイルも表と突き合わせる。**見方は場所で分ける**: **直下は名前**で 1 件ずつ承認し（正当なファイルはごく少数で、「置くだけで配信される」ことがいちばん起きやすい場所）、**入れ物の中は拡張子**で承認する（`js` は TypeScript の出力先なので、名前で承認するとページを 1 つ足すたびに表の更新を要求することになる）。**再帰へ広げる最初の実装は直下を拡張子ベースへ置き換えてしまい、覆う範囲を広げた代わりに直下の保証を落としていた** — `wwwroot/patient-export.js` と `wwwroot/patient-list.css` が全件緑で通った（レビューで実測）。**覆う範囲を広げる変更が、別の軸で範囲を狭めていないかは必ず確かめる。**`wwwroot/lib` だけは CDN 由来の取得物が数百件入るので、理由付きで「中を見ない」と登録して降りない（その判断自体が表の 1 行としてレビューに現れる）。**降りていること自体も別の手がかりで固定する** — 違反 0 件なら緑なので、走査が降りるのをやめても（＝直下しか見なかった頃へ戻しても）全件緑のまま通り、痕跡がどこにも出ない。**表の引き方は「綴りの大小を無視する」が、「中を見ない入れ物」の判定だけは完全一致にする**（issue #270）— 大文字小文字を区別しないファイルシステムでは `wwwroot/FAVICON.ICO` は `favicon.ico` とまったく同じに配信されるので、区別して引くと**正しく配信されている承認済みの資産が「未承認」として報告され**、失敗文言は「表へ足せ」という誤った直し方（同じ資産を 2 度承認する）を案内する。**この非対称は意図したもの**で、「中を見ない入れ物」の判定まで大小を無視すると、大文字小文字を**区別する**ファイルシステムで別の入れ物である `wwwroot/LIB` の中へ降りなくなり、そこへ置かれた PHI が**1 件も列挙されない**（覆う範囲を広げた代わりに別の軸で狭める、issue #254 と同じ形）。**大小だけが違う資産は「未承認」とは別の理由で報告する** — 直し方が「表へ足す」ではなく「綴りをそろえる」だから。ただし**どちらの綴りが正しいかは決め打たない**（ベンダーが配る `LICENSE.txt` のように名前が外部で決まっている資産では表のほうを直すのが正しく、決め打つと登録も改名もできない**行き止まり**になる）。**そろえる先が「中を見ない入れ物」のときはさらに別の理由にする** — 同じ「そろえてください」を出すと `wwwroot/LIB/patients.csv` を `lib/` へ移す手順を教えることになり、**検査は緑になるのにそのファイルは配られ続ける**（失敗文言が PHI を隠す手順を案内する形。実測）。**失敗文言には、その案内がどの項目に掛かるかを書く** — 中身がこぼれ出ると 1 つの文言に「表へ登録します」と「登録して黙らせないでください」が同居し、見出しが無いと上から読んだ人が最初の案内を全項目へ当てる（種別の登録は**すべての入れ物に効く**）。**「中身も一覧に並びます」と約束しない** — 承認済みの種類（`.js` / `.css`）のファイルは並ばないので、並んでいないことを「確認済み」と読むと下の残る境界をそのまま踏む。**こぼれ出た中身は、入れ物とは別の理由にして入れ物と同じ直し方へ寄せる** — 綴りが違う入れ物は「中を見ない」登録に当たらないぶん中まで走査されるので、その中身は「種類が未承認」「入れ物が未承認」として並ぶ。ところが失敗文言は原因ごとの見出しで案内を出し分けるため、中身の見出しの下には**「表へ理由を添えて登録します」**が付き、同じ 1 つの文言の中で「登録して黙らせないでください」と正面から衝突する（`wwwroot/LIB/patients.csv` を実際に置いて再現）。案内どおり `.csv` を種別の表へ足すと、その承認は**すべての入れ物に効く**ので`wwwroot/js/patient-export.csv` が以後ずっと素通りする＝**失敗文言に従うと検出網が一斉に広がる**。**見出しを足した変更が、その見出しを通じて誤った案内を配り始めていないかを必ず確かめる**（案内の出し分けは、相反する案内を同居させない形まで届いて初めて効く）。**付け替えは走査の順に頼らない** — 列挙は入れ物を先に返す実装だが、それに依存すると列挙の書き方を変えただけで中身が元の理由に戻る。**前方一致は区切りまで含めて見る**（`LIB2` の中身が `LIB` の中身として巻き込まれる）。**案内を並べる順も決め切る** — 出現順に任せると、`wwwroot/exports/a.csv` と `wwwroot/LIB/patients.csv` が同時にあるときに「表へ登録します」が先に出て、その後ろに「先にこの入れ物の扱いを決めてから、残りを確認してください」が付く（実測）。上から読んだ人は、先に決めろと言われる前に**すべての入れ物に効く**種別の登録を済ませてしまう。しかも並びは**ファイルシステムの列挙順**で決まるので安定しない。「先にこれを決めてから」と言う案内を必ず先頭へ置き、順そのものを enum から導く対応表で持つ（足し忘れ・使い回しは `UnapprovedCauses_AllHaveTheirOwnTextAndRepair` が落とす）。**見出しの並びも同じ理由で enum の宣言順にそろえる**（1 つの直し方を共有する原因が同時に落ちると、並びが `Directory.EnumerateFileSystemEntries` の順で決まり CI と手元で文言が変わる）。**共有の注意書きは案内ごとに足さず、文言の最後に 1 度だけ出す** — 掛かる案内は同時に 2 つ出うるので、案内ごとに足すと同じ長い 1 文が 2 度並び、「重複を消す」変更で**片方の枝から黙って落ちる**。**名指しに引数の型を載せるときは、素の `FullName` を使わない** — 構築済みの総称型の `FullName` はアセンブリ修飾名（`System.Nullable\`1[[System.DateTime, …, Version=8.0.0.0, …]]`）で、**開けるファイルを指さない**綴りになる。しかもこの repo は絞り込みを `Nullable<T>` で受けることを規約で求めているので、いちばん出やすい引数の形で失敗文言が読めなくなる。`FullName ?? Name` でも足りず、開いた総称（`List<T1>`）は `FullName` を持たないため `List\`1` へ潰れ、`Export(List<T1>)` と `Export(List<T2>)` の名指しが同じになる（どちらも実測）。型引数まで自分で組み立て、名前空間は残しアセンブリ修飾名は載せない。**組み立てでは、入れ子・参照渡し・配列の次元も落とさない**（どれも実測）: 総称型の中に入れ子にした型は `Ns+Outer\`1+Inner` なので**最初の印で切ると入れ子の段ごと落ち**、`Outer<int>.Inner` と `Outer<int>.Other` が同じ綴りになる。`ref DateTime?` は総称でも配列でもない扱いになり**素の `FullName`＝アセンブリ修飾名**へ落ちる。`int[,]` は次元を落とすと `int[]` と同じ綴りになる。**宣言元の型も同じ組み立てを通す** — 通さないと 1 つの行の中で型だけがメタデータの綴り（`GenericSignatureProbeBase\`1`）、引数だけがソースの綴りという食い違いになる。**表示名の組み立てとキーの同一性は分ける**（表示の綴りを変えたときにキーの意味が動かないように）。

    **残る境界**: 入れ物の中は種類での判定なので、承認済みの拡張子を名乗る PHI（患者一覧を `wwwroot/js/patient-list.js` として書き出す等）は拾えない。

    走査するファイルの選び方は、上の 2 つの検査に共通する（どちらも `ScannedSources` を通る）。**パスの形（`Controllers/` ・ `Views/`）で絞らない** — その外に置いた同じコード（`Pages/` のビュー、別フォルダの `partial class`）が素通りする（実測。`RepositoryPaths.EnumerateViewFiles` が同じ形の事故を記録している）。列挙は `.cs` と `.cshtml` の**両方**を回す（`EnumerateWebSourceFiles` は `*.cs` しか返さず、`Views/` 配下に `.cs` は 1 つも無いので、それだけでは「ビューも見る」条件が一度も成立しない＝実測で死んでいた）。照合は**大文字小文字を無視**する（`Response.Headers["cache-control"]` は実際に効く）。**数えるのは「含む行の本数」ではなく「出現回数」**（同じ行に 2 つ書く形は本数だと 1 のままで、配線が増えたことが差分に現れない＝実測）。**許可表との突き合わせもコメントを取り除いた実コードで行う**（元の行で照合すると、§5 が明示的に許している行末コメントを許可済みの行へ足しただけで「表に無い行」「回数 0 回」「許可表の行が実在しない」が同時に出て、原因が読み取れなくなる）。**コメントは取り除いてから判定**する — 行頭だけを見て捨てると、同じ行で閉じたコメントの後ろの実コード（`@* メモ *@ @{ … }`）が素通りする（実測）。逆にコメントで赤くもしない（§5 が求める日本語コメントで CI が落ちる検査は、いずれ検査ごと緩められる）。**読み方は拡張子で分ける。`.cs` は本物の字句解析（Roslyn）が読み、`.cshtml` だけが近似の走査を通る。** 以前は両方を 1 文字ずつ見る自前の走査で読んでおり、「ここはリテラルの開きか」を**綴りの前後から当てて**いた。その当て方が外れる形が 2 つ見つかり、どちらも**見逃す側**だった（issue #249: 開きと見なす直前の文字の集合に `return "…"` / `=> "…"` / `case "…":` が無いため、中身の `//`（URL がこの形）で**その行の残りが落ちる**。issue #252: 改行をまたぐリテラル（`@"…"` / `"""…"""`）の状態を行をまたいで持ち越さないため、2 行目以降が素の実コードとして読まれ、そこに `/*` があると閉じ綴りがリテラルの中にしか無いので**以降のファイル全体がコメント扱い**になる）。**綴りを 1 つずつ足す直し方は取らない** — `>` を集合へ足すと今度は Razor の地の文（`<p>"レベル3 以上</p>`）が開きと判定されて**正しいコードで赤くなる**（向きを逆に踏み直すだけで、近似であることは変わらない）。このファイル自身と `CSharpLiteral` の解説が揃って「次に穴が出たら本物のパーサへ移すこと」と書いており、穴が出たのでそれに従った（`.github/dependabot.yml` をインデントの数え上げではなく YAML パーサで読むようにしたのと同じ形の対処）。判定は `Helpers/CSharpCommentScanner.cs` が正本で、**取り除くのはコメントだけ・リテラルの中身は実コードとして残す**（ヘッダー名は `Response.Headers["Cache-Control"]` のように文字列キーで書かれるため）。**`#if false` で無効化された領域はコメントに数えない**（条件次第で有効になるので、多く報告する側へ倒す）。

    **Razor（`.cshtml`）は C# のパーサでは読めない**（マークアップの地の文に閉じない引用符が普通に現れ、C# として解析するとその 1 文字から先の解釈が総崩れになる）ので、ビューは引き続き**近似の走査**が読む。扱うのは `//` ・ `/* */` ・ `@* *@` と、**`@@`（地の文に `@` を書くための綴り）を 2 文字として読み飛ばすこと**。リテラルは**直前の非空白文字が `=` `(` `,` `[` `{` `:` `?` `+` `@` `$` のいずれか**かつ**その行の中で閉じているか**で開きを判断し、**改行をまたげる 2 種類（逐語的・生文字列）は次の行へ持ち越す**（issue #252 のビュー側。持ち越さないと 2 行目以降が素の実コードとして読まれ、そこに `/*` があるとファイルの残りが飲み込まれる）。

    **この近似を「安全な方向へ」作り替える試みは 2 度とも失敗した。記録しておく。** ①`@* *@` だけを扱う形にすると、マークアップ中の裸の URL の誤認は消えるが、**inline script の `//` コメントが実コードとして読まれ**、§5 が求めるコメントを 1 行書いただけで違反として報告される（`Views/Incidents/Create.cshtml` に実際に数十本ある）。②さらに引用符を追うのをやめると、`= "…@*…"` の形が**逆に見逃し側へ落ちる**（main は拾えていた）。③「閉じないまま終わったら落とす」fail-closed は、表示テキストの `incident_/*.csv` や `support@@*.example.com` で**正しいビューを赤くする**うえ、取り違えた開きを後ろの本物の閉じ綴りが閉じてしまうと何も言わない。**いずれも「誤検知を消すつもりで見逃しを作る／見逃しを消すつもりで誤検知を作る」向きの取り違えで、近似である限りどちらかの穴が必ず残る。**

    **残っている境界（issue #260）**: ビューの走査には、**どれも実測された**次の穴がある。(a) 開きと見なされないリテラル（`return "…"` / `=> "…"`）の中身に `//` があると**その行の残り**が落ちる。(b) マークアップ中の裸の URL も同じく `//` で行が切れる。(c) 表示テキストの `/*` や、開きと見なされないリテラルの中の `@*` は、閉じ綴りが現れるまで（現れなければファイル末尾まで）**黙って飲み込む**。**綴りを足して埋めようとしないこと**（上の①〜③がその試みの記録）。閉じるには `Microsoft.AspNetCore.Razor.Language` でビューを読むしかなく、**そのときも `<script>` の中身は別に手当てが要る**（Razor は JavaScript を解析しないので、`//` のコメントはパーサからは見えない）。移すときは姉妹の走査（`Views.ModelStateKeyPrefixMatchTests.Neutralize` ・ `Helpers/RazorSource`）と共有できるよう `Helpers/` へ置く（いま同じ近似が 3 つある）。
  - **残っている境界**: 許可表は人が判断するエスケープハッチで、「その場所が本当に書いてよいか」は機械では決められない。**エントリが増える差分は、理由の妥当性をレビューで必ず確認する**（`LengthGovernanceExclusions` と同じ扱い）。表そのものにも検査が掛かる（キーと行が実在する・理由が空でない・回数が 1 以上・その行がヘッダー名を含む 等）。**ここに一覧を書き写さない** — 検査を足すたびにこちらだけが古くなるので、正本は `IntendedCacheControlWriters_AreAllRealAndExplained` の本体。
  - **静的アセットの `max-age` は 1 時間に留め `immutable` を付けない** — `_Layout.cshtml` と `_ValidationScriptsPartial.cshtml` が `wwwroot/lib` 配下を `asp-append-version` なしで参照しているため、長期・`immutable` にすると脆弱性修正後も古いファイルが利用者のキャッシュに残り、消す手段が無くなる。統合テストは「配信された値が定数と一致するか」しか見ず**定数の値には恒真**なので、2 つの検査が別々の手がかりで固定する: `StaticAssetCacheControl_MatchesTheDocumentedDirective` が **`docs/security.md` が名乗ると書いている指示と一字一句一致すること**（片方だけを動かす差分が必ず落ちる）。**照合はドキュメント全体ではなく「静的アセットを説明している箇条書き」だけを読み、その中でちょうど 1 件であることを要求する**（issue #265）— 全体を `Match`（最初の一致）で読むと、キャッシュ指示を述べた文が既に 2 つある以上「いま一致しているのはたまたま文言と並び順がそうだから」になり、2 方向に壊れる: **`no-store` 側の説明を「…を名乗ります」と自然に言い換えただけで赤くなる**（実測。何も壊れていないのに落ちる検査はいずれ緩められる）、**手前に同じ値の文が増えると本命の文が誰にも照合されないまま drift する**（実測で全件緑）。切り出しの目印は日本語の見出しではなく**コードの識別子 `OnPrepareResponse`**（`Program.cs` 側の実体そのもので、この 2 つは対で動く）にし、目印が見つからない・箇条書きの外にある・その中で 2 件以上見つかるのはいずれも「どの文を守るのか決められない」状態なので落とす（fail-closed）、`StaticAssetCacheControl_StaysShortLivedAndRevalidatable` が **`immutable` が無く `max-age` が 1 日以下であること**（文書と定数を同時に長期へ書き換える差分を、文書とは独立な手がかりで止める）。**さらに「文書が名乗るキャッシュ指示は 1 つ残らず長期でも `immutable` でもない」を別に要求する** — 上の 2 つは「静的アセットの箇条書き 1 つ」と「定数そのもの」しか見ないので、**目印（`OnPrepareResponse`）を持たない囮の箇条書き**が長期・`immutable` を名乗っても止められなかった（実測で全件緑。issue #265 で塞いだ穴が「目印を持つ囮」から「持たない囮」へ移っただけだった）。運用者が読むのは文書全体なので、どの箇条書きであれその記述が載っていること自体が守りたい状態に反する。**不変条件の本体は 1 か所（`AssertNotLongLived`）に置き、定数側と文書側が同じものを読む**（書き写すと片方が素通りの窓口になる）。**さらに、走査は書式に依存させない** — バッククォートで囲まれた指示しか見ない形だと、囲まずに書いた囮が素通りする（実測で全件緑）。守りたいのは「文書が長期・`immutable` を名乗らないこと」であって書式ではないので、**禁じている綴り自体**（期間の値と、カンマで他の指示とつながった `immutable`）を囲みの有無を問わず走査する。**`immutable` という語そのものは禁じない** — この文書は「長期・`immutable` にはしません」と説明のために正しく使っており、一律に禁じると正しい記述で赤くなる。**「名乗りか反例か」は文章から推し量らず、文書側の機械可読な目印で決める** — 値の直後に `<!--cache-claim-->`（実際に名乗る）か `<!--cache-counter-example-->`（してはいけない例）を置き、**目印の無い指示は fail-closed で落とす**（新しく書いた人が必ず一度どちらかを決めることになり、囮を仕込むには「これは反例です」と差分に書き残す必要がある）。**日本語の言い回しから判定する形へ戻さないこと** — 実際にこの PR の中だけで「6 文字では足りない」「『なく』『ず』が抜けている」「括弧内の否定を拾う」「箇条書き全体から探すと説明文の否定が拒否権を持ち検査が 1 つも走らなくなる」と 4 度踏み直し、そのたびに**正しい文書で赤くなる**か**囮が黙って通る**かを行き来した（CLAUDE.md が別の走査について繰り返し記録している「近似である限りどちらかの穴が必ず残る」と同じ形）。**確かめる単位は「期間の指示 1 つ」ではなく「目印が指している指示の並び全体」にする** — 期間の指示を起点にその 1 つだけを渡していたころは、同じ並びの `immutable` が**一度も見られていなかった**（実測で `public, max-age=3600, immutable` が全件緑で通った）。目印から**同じ行を遡って**並びを切り出せば、期間も `immutable` も同じ 1 回の判定に載る。**ただし切り出した綴りをカンマで「分解」しない** — 遡りは地の文の英単語まで巻き込むので（走査する文字の集合が ASCII の文字と空白を通す）、`CDN adds \`Cache-Control: max-age=31536000\`` は**1 個の読めない token** になり、期間の指示が 1 つも見つからないまま通った（実測で 1 年のキャッシュが全件緑）。**分解の精度に頼らず、切り出した綴りの中から指示そのものを探す**（走査する綴りは目印を要求する側と同じものを使う ——「目印を要求する綴り」と「値を確かめる綴り」がずれると、その差分がそのまま死角になる）。**拾えたかどうかにも空振り検出を置く** — 切り出しの照合は綴りしか見ないので、指示を拾う側が何かの拍子にすべてを取り落としても緑のままになる（実測で、常に空を返す形がその 1 件を足すまで全件緑だった）。文書には静的アセットの期間の指示が必ず 1 件あるので、それが拾えていることを見る。**目印を要求するかの判定は、綴りの種類で分ける** — 両方に同じ規則を当てるとどちらかの穴が必ず残る。**期間の指示**（`max-age=3600`）は値付きで地の文にまず現れないので、**他のヘッダー名を名乗っていない限り**名乗りとして扱う。**その「他のヘッダー」は名前の表（`LifetimeBearingHeaders`）で切る** — 「英数字 + コロンなら何でも別のヘッダー」としていたころは、`CDN:` や `nginx:` のような**ただの見出しを手前に置くだけで名乗りが丸ごと除外された**（実測で 1 年のキャッシュが全件緑）。表に無い名前は除外の根拠にならない（fail-closed。別のヘッダーを文書へ足す人は 1 行加えることになり、その 1 行がレビューに現れる）。**見る範囲は行ではなく箇条書き** — この文書は折り返しが多くヘッダー名と値が別の行へ分かれるので、行だけを見ると**折り返した HSTS で正しい文書が赤くなり**、しかも目印を付けても次の検査で落ちる**行き止まり**になる（実測）。**カンマ隣接の判定も行をまたがない** — 改行も空白なので、条件を付けないと地の文を折り返して書いただけで赤くなる（実測）。**行内の `Cache-Control` やカンマ隣接だけを根拠にしない** — 指示が 1 つだけの名乗り（`Cache-Control: max-age=31536000`）はカンマを持たず、ヘッダー名が**折り返しで前の行へ回った名乗り**は行内に `Cache-Control` を持たないので、どちらも目印を要求される前に読み飛ばされていた（どちらも実測で 1 年のキャッシュが全件緑のまま素通りした。この文書は折り返しが多いので後者は現実的な形）。一方 **`immutable` は単語**なので地の文にも現れ（この文書は「長期・`immutable` にはしません」と正しく使っている）、同じ規則を当てると**正しい文書で赤くなる**ので、名乗りの手がかりがあるときだけ見る。**その手がかりを「同じ行の `Cache-Control`」にしてはいけない** — ヘッダー名が折り返しで前の行へ回った名乗り（`` `Cache-Control:` `` の次の行に `` `immutable` ``）が目印を 1 つも要求されなかった（実測で全件緑。`immutable` は RFC 8246 上 `max-age` 無しでも成立するので、これは期間の指示と同じ重さの名乗り）。**かといって範囲を箇条書きへ広げるだけでは正しい文書が赤くなる** —— この文書は同じ箇条書きの中で「`Cache-Control: public,max-age=3600` を名乗ります（…長期・`immutable` にはしません）」と説明している。**そこで「手前の名乗りが既に目印で閉じられているか」で切る**: あいだに目印があればその名乗りは決着済みでここまで掛からず（上の地の文がこれ）、目印が無ければ名乗りの続きとみなす（折り返した名乗りがこれ）。目印は囲みの中にあるものも数える（この文書は約束ごとの説明として目印を囲みで掲げており、数えないとその説明を含む箇条書きの `immutable` が赤くなる）。カンマ隣接も引き続き手がかりにする。**走査する綴りに `immutable` を含める** — 期間の指示だけを走査していたころは、`public,immutable`（RFC 8246 上 `max-age` 無しでも成立）を名乗る文が目印を 1 つも要求されず判定にも渡されなかった（実測で全件緑）。**目印を探す読み飛ばしは行をまたがない** — またぐと次の行に置かれた別の指示の目印を借り、目印の無い指示が反例として見逃される（実測）。**目印は「コードの囲みの外」にあるものだけを認める** — 文書はこの約束自体を説明するために目印を囲みで掲げるので、認めると**正しい文書で赤くなり**、逆に囲みの中を一律に逃すと「指示を囲みで閉じ、目印を別の囲みへ入れる」形で囮が通る（実測）。判定は「行頭からその位置までのバッククォートの個数が奇数か」の 1 つに寄せ、名乗り側と目印側の両方が同じ規則を読む。**ヘッダー名は「一番近いもの」を見る** — 先頭から探すと、同じ綴りの手前に別のヘッダー名があるだけで（`Vary: Accept-Encoding, Cache-Control: …`）**名乗りが丸ごと除外される**（実測で 1 年のキャッシュが全件緑のまま通った）。**名前を探す範囲は「指示の並びとして地続きの綴り」だけにする** — 箇条書き全体を見ていたころは、同じ箇条書きの中で**先に別のヘッダー名を書いておくだけ**で後ろの 1 年のキャッシュが目印も判定も要求されずに済んだ（実測で全件緑。まず「HSTS の値も書いた囮」で見つかり、「名前と指示のあいだに別の期間の指示が無いこと」を足しても**値を書かずに名前だけ言及する囮**で同じことが起きた ——範囲を広げるほど「手前に置くだけ」の囮が作りやすくなる）。ヘッダー名と値のあいだに地の文を挟まないことを要求すれば、この抜け道は綴りに依存せず閉じる。**代償は「折り返して名前と値を別の囲みへ分けた記述は目印を要求される」**ことだが、この文書は既に名前と値を続けて書く形を慣習にしており（`Cache-Control` の 2 件がそう）、そちらへそろえれば通る＝行き止まりではない（失敗文言と `docs/security.md` の両方がその直し方を案内する）。**地続きの中でも「名前と指示のあいだに別の期間の指示が無いこと」は要求する** — カンマでつなげて `Strict-Transport-Security: max-age=…, public,max-age=…` と書けば後ろの名乗りを除外できてしまうため。**名前とコロンのあいだの閉じのバッククォートは許す**（`` `Strict-Transport-Security`: `` はごく普通の書き方で、許さないと正しい文書が赤くなる＝実測）。**改行は読み口（`ReadSecurityDoc`）で LF へそろえる** — この repo に `.gitattributes` は無く、Windows の既定では CRLF になる。すると空行の行頭が CR になり、「空白だけの行は継続行」という判定が空行まで継続行と読み、箇条書きの境目が消えて照合の範囲が黙って広がる（判定を 1 箇所ずつ直すと取りこぼしが出るので、読み口 1 つでそろえる。§10）。**囲いは「行の頭にあるもの」だけを数え、奇数なら fail-closed で落とす** — どこに現れた ``` も数える形だと、**地の文で綴りに言及しただけで偶奇が反転し、それ以降の文書全体が「ブロックの中」になって検査が丸ごと黙る**（実測で全件緑。しかも空振り検出は囲いより手前の指示 1 件で満たされるので拾えない）。**Markdown の形を読む走査は `Helpers/MarkdownSource.cs` に集める** — 行の切り出し・囲みの内外・囲いの内外・箇条書きの境目はキャッシュ指示と無関係な汎用の近似で、テストクラスの private に置くと次に文書を読むテストが**穴ごと複製する**（この repo が既に 3 つの姉妹の近似を抱えているのと同じ形）。**読み口も共有する**（`MarkdownSource.Read`） — パスだけを共有して読み方を各自で書くと、改行の正規化を足した側だけが直り、もう片方は Windows のチェックアウトでだけ壊れる。**残る境界: コードブロック（``` で囲んだ設定例）の中は見ない** — あそこでは HTML コメントがそのまま表示されるので、目印を要求すると「正しい短期の nginx 設定例を足しただけで CI が赤くなり、案内される直し方に従うと運用者がコピペする設定へコメントが混ざる」（実測で赤くなった）。代償として、**ブロックの中に長期のキャッシュを書いても検査は黙る** — 前段の設定例を足すときはレビューで見る。**この代償と「名前と値は続けて書く」という要求は、`docs/security.md` 自身にも書く**（レビュー指摘）—— あの節は「目印の付いていない指示を落とします」と書いており、書き手が読むのはこの CLAUDE.md ではなくあちら。境界を書かないと、囲みの中に 1 年の設定例を足した人が「CI が守っている」と誤解する。空振り検出は 2 つ残す: **兄弟の検査が固定している静的アセットの指示が「確かめた中」にあること**と、**目印を要求する枝が 1 件は見ていること**（どちらも違反 0 件では緑になってしまうため。実測で、文脈の判定を常に false にすると後者が落ちる）。**箇条書きの境目の規則は `BulletBounds` の 1 か所に置く**（2 か所へ書き写すと、記号を変えたときに片方が無関係な範囲を見たまま静かに誤分類する）。**上へたどってよいのは「継続行（字下げされた行）」の間だけ** — 条件を付けずに直前の `- ` まで遡ると、**箇条書きの外にある地の文**が手前の箇条書きの一部として扱われ、「箇条書きの中にあること」の検査が原理的に落ちなくなる（実測）。**運用手順が引用する診断の綴りは定数にして文書と突き合わせる** — `Program.cs` の literal のままだと、言い回しを変えた瞬間に手順の grep が永久に空振りし、全許可のまま動いている配備や診断が静かに失敗している配備が「きれい」と読める。**対象は診断の失敗 2 本だけでなく、運用手順の主役である主警告 2 本（全許可 / 一致しえない項目）も含む** — 以前は主警告の綴りが本体・テストの両方で literal の写しになっており、**文面を推敲するとテスト側の写しを直すだけで緑に戻り、`docs/security.md` の引用だけが黙って古くなる**状態だった（テスト側の目印も本体の定数を読む形にしてある）。**ここに定数名を書き並べない** — 増えたときにこちらだけが古くなるので、正本は `AllowedHostsWarningReporterTests.DiagnosticMarkers_AppearInTheOperatorRunbook` の `[InlineData]` の並び。**書き出しだけを突き合わせるのは意図的**で、文面には差し込み（`{Environment}` / `{Count}`）が入る一方、文書側はそこを `…` や `N` と書いて読み手に説明するため、全文を照合すると**文書が読みやすく書いてあるというだけで赤くなる**。そのぶん**目印より後ろの言い回しの drift は規約とレビューで守る**。**診断の失敗を記録するはしご（通常のログ → 標準エラー → 何もしない）は 1 か所に寄せる** — 呼び出し口が「検査そのものの失敗」と「購読を張れなかった失敗」の 2 つあり、書き写すと片方にだけ守りが残る（実測でそうなっていた）。**出せなかった警告は「値を覚えない」のではなく、出せなかった側だけを旗で持ち越して retry する** — 覚えない形だと、片方が失敗し続ける限り**成功したほうの警告まで**再読み込みのたびに出し直され、重複抑止を置いた理由そのものが崩れる。
- **時刻は常に `IClock`**（`Services/IClock.cs`、JST）。`DateTime.Now/Today/UtcNow` を直接呼ばない。永続業務タイムスタンプと `AuditLog.ChangedAt` は運用ローカル時刻(JST)で保存。新規 View では `@inject IClock Clock` ＋ `Clock.Today` を使う。
- **EF Core 本体とプロバイダ実装はメジャー版を全プロジェクトで揃える** — プロバイダ実装（Sqlite / SqlServer / Npgsql）は EF Core の内部 API に結び付くため、本体と同じメジャー版でしか動作保証がない。一方 `Microsoft.AspNetCore.Identity.EntityFrameworkCore` と `Microsoft.Extensions.Diagnostics.HealthChecks.EntityFrameworkCore` は公開 API しか使わず、メジャー版は .NET のリリース（`net8.0` → `net9.0`）に追随する（9.0.0 は `net9.0` 専用）ため、この規則の対象外にしている（束ねると EF Core 9 への更新が復元不能になる）。一部だけ上げてもビルドもテストも通る（テストは InMemory / SQLite しか触らない）ため、**PostgreSQL 配備でだけ実行時に壊れる**。Dependabot は `.github/dependabot.yml` の `nuget-ef-core`（通常の版更新）と `nuget-ef-core-security`（セキュリティ更新。`applies-to` の既定は version-updates なので別グループが要る）が major も含めて 1 本の PR に束ね、版ズレ自体は `EfCorePackageAlignmentTests` が検出する。検査は `packages.lock.json` の解決済み版を読む（壊れる主役は推移依存の `Relational` で、どの csproj にも直接書かれていないため）。「EF Core 系とは何か」の定義は dependabot.yml の `patterns` が唯一の真実の源で、名前に `EntityFrameworkCore` **または** `EFCore`（`EFCore.NamingConventions` 等）を含むパッケージがパターンから漏れていないことも同テストが固定する（上記 .NET リリース列の 2 つだけは理由付きで明示除外）。**プロバイダを足すときは 3 箇所（上記 2 グループの `patterns` と `nuget-minor-and-patch.exclude-patterns`）すべてに反映する。** 2 グループの `patterns` は集合として一致している必要があるが、`exclude-patterns` 側は書き方を問わず「EF Core 系を 1 つも拾わないこと」だけを求める。判定は文字列の一致ではなく、解決済みのパッケージ ID と `patterns` が表す範囲の代表値の両方が拾われないことで行う（`patterns` 側が前方一致 `"Foo*"` のときは、除外もその前置詞で始まるものをまるごと覆う必要があり、`"Foo"` と `"Foo.*"` のようにドット区切りの形を列挙する書き方では `FooBar` 等の同族が漏れるため足りない。完全一致のパターンは 1 つの ID しか指さないので、その ID を覆えていれば足りる）。そのため`"Npgsql*"` のように広く書くのは可、逆に `"Microsoft.EntityFrameworkCore*"` から末尾 `*` を落とすと（`.Sqlite` / `.SqlServer` / `.Relational` など既存のプロバイダ実装がその時点で漏れるため）通らない。広げすぎて上記 .NET リリース列の 2 つまで minor / patch の束ねから外すのも同テストが検出する。
- **SQL Server の ADO.NET ドライバ（`Microsoft.Data.SqlClient`）のメジャー版は EF Core の SqlServer プロバイダに合わせる** — このドライバは `Microsoft.EntityFrameworkCore.SqlServer` の実体で、動作保証があるのはプロバイダが宣言したメジャー版に対してだけ。ところが名前に `EntityFrameworkCore` / `EFCore` を含まないため上記の `nuget-ef-core` グループには入らず、メジャー更新が単独 PR として現れる（実際 PR #178 が 5.1.7 → 7.0.2 で出ていた）。**壊れ方は EF Core 本体とプロバイダの版ズレとまったく同じ**で、テストは InMemory / SQLite しか触らないため**ビルドも全テストも緑のまま通り、SQL Server 配備でだけ実行時に壊れる**。`.github/dependabot.yml` の nuget エコシステムの `ignore` が major 更新だけを保留し（minor / patch は csproj の「床値」を上げる更新なので止めない）、`EfCorePackageAlignmentTests` が 5 つを検出する: 解決版と EF Core 宣言版のメジャー不一致 / 床値の後退 / **ピンが直接参照でなくなること**（`packages.lock.json` の `type` が `Direct` から外れる = csproj のピン削除や推移依存への差し戻し）/ 保留の消失 / 保留の効きすぎ（`update-types` の欠落・`versions` の追加・重複エントリ）。**期待値はテストに書かず EF Core の宣言側（`packages.lock.json` の `dependencies`）から読む**ので、EF Core を上げれば検査が自動で追随する。**ただし「床値の後退」の比較相手はあくまで EF Core の宣言（現在 5.1.9）であって、csproj に書かれたピンの値ではない**——ピンを削除した場合は上記の `Direct` 検査が捕まえるが、**ピンの値を「EF Core の宣言版以上・現在のピン未満」の範囲（今なら 5.1.9 以上 5.2.3 未満、たとえば 5.2.0 へ下げる変更）へ下げると 5 つすべてを素通りする**。床値検査が比べるのは EF Core の宣言であって直前のピンではないため、`5.2.0 >= 5.1.9` が成立してしまう。期待値を書かない方針と引き換えに残している穴なので、**ピンの値が下がる差分は（下げ幅の大小によらず）レビューで理由を確認する**。ドライバのメジャーを上げたいときは、それを宣言する版の `Microsoft.EntityFrameworkCore.SqlServer` へ同じ変更セットで上げる。
- **`SameDepartmentHandler`（`Authorization/SameDepartmentHandler.cs`）は `Incident` の eager-load が前提**。`PreventiveMeasure` / `CauseAnalysis` で `AuthorizeAsync` する前に `.Include(x => x.Incident)`。**fail-closed**（`Incident` が null なら拒否）。

### 新規 POST アクション追加時のチェックリスト

1. `[Authorize]`（または適切なポリシー）を付与。
2. `[ValidateAntiForgeryToken]` を付与（View には `@Html.AntiForgeryToken()`）。
3. `ConcurrencyToken` を hidden field で round-trip し `OriginalValue` にピン。
4. `SameDepartmentHandler` が必要なら `.Include(x => x.Incident)`（`IncidentControllerHelpers.IsAuthorizedForAsync` 経由でも同様）。
5. `SaveChangesAsync` を使い `ExecuteUpdate` / `ExecuteDelete` を使わない。
6. 成功時 `TempData["Success"]`、失敗時 `TempData["Warning"]`。
7. 新しい自由記述/個人名カラムには `[Sensitive(...)]` を付与（平文で残してよい列なら理由付きで `[NotPhi("...")]`。どちらも無い `string` 列は `AuditedEntityPhiClassificationTests` が落とす）。
8. 対応するテストを `tests/` に追加（`InMemory` DbContext 優先）。

### セキュリティ（固有事項）

- **部署スコープは Create/Edit でサーバ側強制**（issue #63）。Staff は自部署のみ登録/編集でき、フォームの部署値は `EnforceOwnDepartmentForStaff` で本人の `AppClaimTypes.Department` に上書き。Admin/RiskManager は全部署可。
- 認証 Cookie は `HttpOnly=true`, `SameSite=Strict`。Production は HTTPS リダイレクト＋HSTS 有効。
- **リバースプロキシ配下では `AllowedHosts` を実ホスト名に絞り、転送ヘッダを有効化**（issue #64）。既定 `"*"` のままにしない。**「絞れているか」の判定は `Models/Validation/AllowedHostsPolicy.IsPermissive` が正本で、ワイルドカードを 1 つでも含めば全許可と見なす** — `HostFiltering.IsTopLevelWildcard` は **`*`（HTTP.sys）・`[::]`（IPv6 Any）・`0.0.0.0`（IPv4 Any）の 3 つ**を全許可として扱い（**綴りはこの 3 つで、素の `::` は含まれない** — 実測で `AllowedHosts=::` はどのホストも 400。「直すとワイルドカードになる」の判定でここを取り違えると、`" ::"` の空白を外すと全許可だと**事実と違うこと**を名指しして言うことになる。issue #269）、1 つでもあれば「空でない Host はすべて受け付ける」へ切り替わるため、`"*;incident.example.com"`（実ホスト名を「追加」しようとすると自然に書く形）は**絞れていない**。以前の判定は値全体が `"*"` と一致するかだけを見ていたのでこの綴りで警告が出ず、`docs/security.md` が案内している「警告ログが出ていないことを確認する」手順が**そのまま誤った安心**になっていた（実測で `*;real` と `0.0.0.0;real` がどちらも別ホストを 200 で受けることを確認済み）。**判定はホスト名の正規化（IDNA / NFKC）の後に行う** — フレームワークは各項目を `new HostString(entry).ToUriComponent()` に通してから 3 綴りと突き合わせるので、全角の `０.０.０.０` や読点の `0。0。0。0` も全許可になる（実測）。生の `Ordinal` 比較のままだとそこが「警告の出ない全許可」として残る。正規化できない綴りは判断できないので警告する側へ倒す（過剰に警告する＝安全側）。**「1 件も残らない値」も全許可**として扱う — 汎用ホストの既定設定は空の項目を落とした結果が 0 件なら `["*"]` へ落とすので、`;` / `;;` は全許可（実測で別ホストを 200 で受ける）。**だから判定はフレームワークの既定設定をそのまま写し、独自に丸めない** — とくに**先にトリムしない**こと。ホスト名の検査は項目をトリムしないので、前後に空白のある項目は**どの `Host` とも一致しない**（実測で 400）。トリムするとこれらが「全許可」に見え、**一致していないだけの設定を素通りと呼ぶ**ことになる。**ただし「落ちるからすぐ気づける」は成り立たない** — `AllowedHosts=a.example.com; b.example.com`（一覧を書くときに自然に入る形）は実測で**1 件目が 200・2 件目が 400**、つまり**サイトは生きたまま特定のホスト名だけが静かに落ちる**。しかも `IsPermissive` は正しく `false` を返すので、`docs/security.md` が案内する「警告が出ていないことの確認」が**そのまま誤った安心**になる（`*;real` を取りこぼしていた頃と同じ形の穴で、向きだけが逆）。**だから警告は 2 本ある**: 「黙って素通りする」形を `IsPermissive` が、「並べたのに一致しない」形を `NeverMatchingEntries` が拾う。**片方だけを足した状態にしない**（どちらの穴も運用者からは「警告が出ていない」という同じ見た目になる）。**「一致しえない項目」の理由は enum `DeadEntryReason` が正本**（**ここに一覧を書き写さない** — 理由を足したときにこちらだけが古くなる。issue #256 / #269） — 突き合わせは「リクエスト側の `Host` からポートを落とす」→「許可リストの項目と**そのまま**比べる」の 2 段なので、項目の側に空白が残っていてもポートが付いていても一致しえない（`ASPNETCORE_URLS` からホスト名を写すとポートごと持ってくるのは `0.0.0.0` を写すのと同じくらい自然な形で、実測ではポートを付けて送っても 400）。**ポートの判定を「コロンがあるか」で書かない** — 角括弧の IPv6 リテラルはコロンを含むがポートを持たず、拾うと今度は*生きている項目*を「消してよい」と案内する側（＝警告が障害を作る側）へ倒れる。判定はフレームワーク自身のホスト部の切り出しへ委ね、`HostString.Port` は見ない（ポート部が数値として読めない綴りで `null` になり取りこぼす＝実測）。**ただし「ホスト部と食い違う＝死んでいる」から先の理由を 2 択の当て推量で決めない**（issue #269） — ホスト部の切り出しは `]` を含まず**コロンが 2 つ以上**ある値を IPv6 かどうかに関係なく角括弧で包むので、「角括弧を足されたか」だけで決めると `www.example.com:8080:`（末尾コロンのタイプミス、`host:port:path` の写し）が**「角括弧で囲め」と案内され**、従っても直らない（並べたかったホスト名は 400 のまま）のに、以前はその囲んだ綴りが**「一致しうる項目」と見なされて 2 本目の警告だけが消えて**いた＝**警告が自分で自分を黙らせる**形。**角括弧が意味を持つのは素の IPv6 リテラルだけ**で、それ以外を囲んだ綴り（`[www.example.com:8080:]` ・ `[foo]` ・ `[fe80::1%eth0]` ・ `[::1 ]`）は**本物の Kestrel が `Host` ヘッダーとして 400 で弾く**（実測）。**それでも「素の IPv6 でなければ死んでいる」とは書かない**（レビュー指摘） —— 実測では `[a:b]` ・ `[...]` ・ `[::1::2]` ・ `[0.0.0.0]` が**どれも 200** で、Kestrel の受け付け方は「IPv6 として正しいか」とは無関係だった。そう書くと**実際には一致する項目を「消してよい」と案内する**側（見逃しより重い誤り）へ倒れ、かといって Kestrel の文字集合を推測で写すのは「近似を育てる」道そのもの。**判定は実測で 400 になった原因の文字だけに絞る**（現在は空白・`%`・`/` の 3 つ。**正本は `ContainsSpellingAHostHeaderCannotCarry` の docstring で、ここに一覧を書き写さない** —— 文字を増減したときにこちらだけが古くなる。実際、`/` を足したあともこの括弧の中は「空白と `%`」のままで、読み手が `/` を根拠の無い追加と誤解しうる状態になっていた）。いずれも Host ヘッダーの構文として運べないので誤検知の余地が無い。**どれも角括弧の有無を問わず**弾く（レビュー指摘）。空白は `0.0.0 .0` のような途中の形も同じ（角括弧の中だけを見ていた頃は無警告で、運用者がその空白を外すと `0.0.0.0` ＝全許可になり、`www.example .test ` は前後の空白の警告に従って直した先が**無警告のまま 400** だった）。**`%` も同じ** — 一時は「普通のホスト名では percent-encoding として合法だから角括弧の中だけ」としていたが、**実測はその逆**で `a%2Db.test` ・ `www.example%2Ecom` ・ `a%b.test` ・ `a%25b.test` はどれも 400（比較: `a_b.test` ・ `a~b.test` ・ `xn--bcher-kva.test` は 200）。角括弧の中だけを見ていた版では `…;www.example%2Ecom` が**警告 2 本とも出ないまま 400** になっていた。**推測ではなく実測で決めること** —— この 1 行は「合法そうだから」という理屈だけで穴になっていた。この 1 行で、docs/security.md が『括弧の内側へ入った空白は名指しできない』と断っていた見逃しも同時に閉じた（`[foo]` 等は引き続き見逃す。**運用者をそこへ誘導しないことが唯一の守り**なので、`NotABareHostname` の文面から「角括弧で囲むな」を外さない）。**スコープ付き（`fe80::1%eth0`）を IPv6 と数えない**のも同じ実測に基づく ——囲んでも Kestrel が弾くので「囲めば一致する」が事実にならない。**除き方は `IPAddress.ScopeId` ではなく綴り（`%` を含むか）で見る** —— `TryParse` はスコープ名が解決できないと `ScopeId` を 0 にして成功するので、`ScopeId` を見る形は**実行機のインターフェース表に依存**し、同じ設定値の分類が配備先ごとに変わる（§11「特定 OS でしか通らないテストを作らない」にも反する）。逆に角括弧を足されない綴りを一律ポート扱いすると、`a]b.test` のようなポートを 1 つも含まない項目に「ポートを外せ」と案内する。**ポートと名乗るのはホスト部の直後がコロンのときだけ、IPv6 と名乗るのは `IPAddress` で実際に読めるときだけ**にし、どちらとも言えない綴りは `NotABareHostname` へ倒して**原因を断定しない**。**空白や `%` が残っている項目は、その手前でそれぞれ専用の理由を名乗る**（レビュー指摘）— `NotABareHostname` の文面は「角括弧で囲んでも直らない・余分なコロンを書くな」と案内するので、正規化で空白が括弧の内側へ移っただけの `" ::1"` に付けると**事実と逆**になる（正しい直し方はまさに空白を外して `[::1]` と書くことで、コロンは消してはいけない）。`%` も同じで、`www.example%2Ecom` にはポートも余分なコロンも無い。**`RepairedSpellings` は直し方を組み合わせて当てる**（レビュー指摘）—— `0.0.0.0%20` は読めない末尾を削れば `0.0.0.0` ＝全許可なのに、空白だけを落とす形では `WildcardOnceRepaired` に当たらず**ごく普通の「実ホスト名へ直せ」**が付いていた（`[::]%20` は括弧のおかげで当たっていたので**非対称**でもあった）。**直し方は 1 通りに決めず、組み合わせた候補をすべて見る**（レビュー指摘）—— 「`%` 以降を削る」「`%` を抜く」「スキームとパスを外す」「正規化が補った角括弧を外す」の組み合わせ。1 つずつ別々に当てていた頃は、2 つ以上が要る綴り（`http://0.0.0.0%20` ・ `%0.0.0.0/0` ・ ` ::%20` ・ `//0.0.0.0`）が専用警告から外れ、**ワイルドカードの注意を持たない文面**が付いていた。候補はたかだか十数通りなので数え上げても代償が無く、多く報告する側へ倒れるので誤検知の害も無い。**候補には「コロンを落とす」向きと「コロンの手前を落とす」向きの両方が要る**（issue #269）—— ホスト部の切り出しは `]` を含まず**コロンが 2 つ以上**ある値を*丸ごと*角括弧で包むので、`0.0.0.0:8080:` ・ `0.0.0.0::` ・ `0.0.0.0:8080:9090`（末尾コロンのタイプミス、`host:port:path` の写し）では `ComparableSpelling` がポートを 1 つも落とせず、`NotABareHostname` の文面（「ポートも余分なコロンも書くな」）に従うと `0.0.0.0` ＝全許可になるのに注意が付いていなかった（`TruncateAtFirstColon`）。逆に `http:0.0.0.0`（`//` の打ち損ね）はホスト部が `http` になるため URL 用の直し方（`://` と `/` を見る）では届かず、同じ形の穴になっていた（`AfterBareScheme`）。**コロンが 1 つの `0.0.0.0:8080` は正しく警告されていたので、どちらも「同じ種類のタイプミスで注意の有無が分かれる」形**だった。**スキームと見なすのはコロンの手前が ASCII の英字だけのときに限る** —— 広げると `b.example.test:0.0.0.0` ・ `192.168.0.1:0.0.0.0` のような実ホスト名まで「直すとワイルドカードになる」と名指しする（この境界は `InspectNeverMatchingEntries_NamesWhyEachEntryCannotMatch` の 2 件が固定する。**実測でも、この 2 件を足すまでは条件を外した変異が全件緑のまま通った**）。**候補は「順番を決め打った組み合わせ」ではなく閉包で数え上げる**（issue #269）—— 以前は「空白と括弧」→「`%` の扱い」→「URL / コロン」という決め打ちの入れ子で当てていたため、**同じ手を 2 度使う綴りや順番が逆の綴り**が漏れていた（`http:0.0.0.0:8080:` は「スキームを外す」→「コロンから先を落とす」の順が要る）。1 手ずつ適用して新しい綴りが出なくなるまで広げれば、順番も回数も決め打たずに済む。どの手も綴りを**伸ばさない**ので閉包は必ず有限だ（**閉包がどこまで広がるかの実測値をここに書かない** —— 以前ここには写しが残っていて正本と食い違っており、その数字を信じると `truncated ||` の fail-safe を「届かないから不要」として消しうる。値と「上限に届きうるか」は下記のとおり `MaxRepairedSpellings` の docstring が正本）が、起動時と設定の再読込で走るので上限（`MaxRepairedSpellings`）を置いてある。**上限で打ち切ったときは「見つからなかった」ではなく「判断できない」として警告する側へ倒す**（ただし**「打ち切った」と呼ぶのは広げる先が残っているときだけ** —— 待ち行列を見ずに件数だけで決めると、ちょうど上限で数え終わった閉包まで打ち切り扱いになり、**数え上げが「ワイルドカードにはならない」と示した項目**を「なりうる」と名指しする。倒れる向きは安全側でも、名指しした項目について事実と違うことを言っているのは issue #256 と同じ誤り） —— 見つからなかった扱いにすると、上限に届くような綴りだけがワイルドカードの注意を持たない文面になり、**上限そのものが fail-open の口**になる。**上限の値と「どこまで届きうるか」の実測は `MaxRepairedSpellings` の docstring が正本で、ここに数字を書き写さない**（issue #273。以前ここには「上限 256 に対し実測の最大が 46 なので打ち切りには届かない」と書いてあったが、値も結論も後から覆っており、**CLAUDE.md 側だけが古いまま残っていた** —— この repo には `CLAUDE.md` と定数を突き合わせる検査が無いので、ずれても全件緑のまま。**「打ち切りには届かない」と読んで `truncated ||` の fail-safe を落とすと、上限そのものが fail-open の口に戻る**）。**上限に届く綴りは本物の項目を並べたテストでは作りにくい**ので、判定を純粋関数 `RepairsToWildcard(candidates, limit)` へ出し、`RepairsToWildcard_TreatsATruncatedSearchAsUndecided` が**合成した候補の並び**で両方向を固定する。**配線（数え上げ側が旗を立て、判定側がそれを見る）は上限を引数に取る入口で実際に走らせる** —— 本番の上限に届く綴りを並べたテストは無いので、固定のままだと旗を立てるのをやめても全件緑になる（実測）。「途中で打ち切る」側と「ちょうど数え終わる」側の両方を別々のテストで固定する（前者しか無いと、待ち行列を見る条件を外す変異が通る）。**手を足すときは「この手で新しくワイルドカードに当たる項目はどれか」を必ず実測する** —— 末尾のポートだけを外す手（`WithoutTrailingPort`）は、はじめ `IsIpv6Literal` の門番を付けて「`::1` を `::` に化けさせない」としていたが、**9,549 通りで 1 件も結果が変わらなかった**（外すときに最後のコロンごと落とすので、`::1` から残るのは `:` であって `::` ではない）。どのテストにも守られない門番は、読み手に守られていると誤解させるだけなので消した（§6）。**候補を足す差分は、増やす方向にしか効かないことを実測で確かめる** —— 9,549 通りの綴りで直前の実装と突き合わせ、796 件が `WildcardOnceRepaired` へ移り、そこから外れた項目も生死が変わった項目も 0 件であることを確認した。**ただし直し方のモデルは原理的に完全にならない**ので、原因を言い当てられない受け皿（`NotABareHostname`）の文面にも「直した結果がワイルドカードなら書くな」という注意を入れてある（`:::8080` のように、閉包が拾えても拾えなくても運用者が同じ穴に落ちうるため）。**`WildcardOnceRepaired` の文面は「必ずそうなる」とも断定しない** —— 直し方は複数あり、そのうち 1 つが着地するだけ（`0.0.0.0:8080:` はポートだけ外せば `0.0.0.0:8080` で、これはワイルドカードではない）。上限で打ち切った項目も「判断できない」側からここへ倒れるので、断定すると名指しした項目について事実と違うことを言う形（issue #256）になる。**`WildcardOnceRepaired` の文面で原因を 1 つに決め打たない** —— 「空白かポートを外すな」と書いていた頃は、`0.0.0.0/0` ・ `%0.0.0.0` ・ `http:0.0.0.0` のようにどちらも持たない項目に対して**注意が自分には当てはまらないと読める**状態だった（issue #256 が名指しした誤りと同じ形）。**「運べない」のは文字だけではなく、角括弧の<b>使い方</b>も含む**（issue #269。実測: `Host` が角括弧を運べるのは先頭に開いて 1 度だけ閉じる形（`[::1]` ・ `[::1]:8080` は 200）だけで、`[0.0.0.0` ・ `0.0.0.0]` ・ `[*` ・ `*]` ・ `a[b.test` ・ `[[a]]` はすべて 400）。以前はこれを見ていなかったため、**`[0.0.0.0` は警告 2 本とも出ないまま毎リクエスト 400** になり、しかも運用者が余計な括弧を消すと全ホスト許可だった。さらに ` [0.0.0.0` は前後の空白の警告が付くので、**案内どおり空白を外すとその無警告の状態へ着地**した（「警告に従って直した先が、無警告のまま 400」の形）。**判定は「1 組・先頭・閉じが後」だけを見て、中身には触れない** — `[a:b]` は 200 なのに `[foo]` は 400 で、中身から受け付け方は言い当てられない。広げると*実際には一致する項目*を「消してよい」と案内する側（見逃しより重い）へ倒れる。**運べない文字には `/` も入る** —— `AllowedHosts` へ**アドレスバーから URL ごと貼る**のは自然な間違いで、ポート付きの URL は正規化で `[https://…:8443]` になり「正規化後の綴り＝ホスト部」に化けるため、`/` を見ないと**警告 2 本とも黙ったまま**その名前が 400 になる（実測で `Host: https://…` も `a/b.test` も `a.test/` も 400）。**綴りを足すのは実際に運用者が書く形が見つかったときだけ** —— `?` ・ `@` ・ `#` ・ `,` も同じく 400 だが足していない（足すほど「この版の Kestrel ではこうだった」という主張が増える）。**運べない文字を足したら、直し方のモデルも同じ変更セットで足す**（レビュー指摘）—— `/` を足したことで `http://0.0.0.0:5000` ・ `0.0.0.0/0` ・ `::/0` が新たに死んだ項目になったのに、直し方が空白と `%` しか見ていなかったため `WildcardOnceRepaired` から外れ（**ただし `::/0` は直した先が `::` ＝ワイルドカードではないので、いまも `UrlInsteadOfHostname` のまま正しい**。実測。ここを「3 つとも専用の理由が付く」と読むと `docs/security.md` に事実と違う案内を書くことになる）、**`ASPNETCORE_URLS` をそのまま貼るほうがスキームを外した `0.0.0.0:5000` より弱い案内になる逆転**が起きていた（`HostnameInsideUrlLikeSpelling` が 3 つ目のモデル。正規化が補った角括弧を外してから見る必要がある）。**URL の形には専用の理由を付ける** —— `https://b.example.test` は `https:` がポート区切りに見えるため、分けないとポートを 1 つも含まない項目に「ポートを外せ」と案内することになる。**ただし「これは URL だ」と断定できるのは `/` があるときだけ**（issue #269）—— `//` を打ち損ねた `https:b.example.test` と、ごく普通の `localhost:8080`（英字だけの単一ラベル＋本物のポート）は綴りの形が同じで、どちらかへ寄せると必ず片方に**事実と違う理由**が付く。そこで**分類は増やさず、ポートの文面のほうから「ポートを含む」という断定を外した** —— 仕組み（「コロンから先が残っているので一致しえない」）と**両方の読み方の例**を書き、どちらに当たるかは運用者が自分の項目を見て決める。**同じ理由で「コロンの後ろに文字がある」とも断定しない**（issue #274）—— `a.test:` ・ `[::1]:` のように**末尾がコロンだけ**の綴りも同じ分類に入る（`AllowedHosts=${HOST}:${PORT}` で `PORT` が未設定だと残る形で、`ASPNETCORE_URLS` を写すのと同じ種類の事故）。断定すると運用者は**在りもしないポート番号を探す**ことになり、issue #256 が名指しした「名指しした項目について事実と違うことを言う」誤りそのものになる。**文面は「その分類に入りうる綴りすべてについて真であること」だけを守る** —— 分類に入る綴りの側は `InspectNeverMatchingEntries_NamesWhyEachEntryCannotMatch` の `[InlineData]` が覆う（末尾コロンの形も 1 件ある）が、**散文が真かどうかは機械では確かめられないので、文面を変える差分はレビューで見る**。**綴りから読み方を当てる検出網を足さないこと**（issue #256 が名指しした誤りを、向きを変えて作り直すだけになる）。**実測の射程は Kestrel**（レビュー指摘）—— 空白と `/` は `Host` ヘッダーの構文としてどこでも無効だが、`%` は RFC 3986 の reg-name としては合法なので、IIS / HTTP.sys の前段に置く配備では理屈のうえで一致しうる。それでも弾くのは**実在する DNS 名に `%` は現れず**ブラウザも `Host` を percent-encode しないためで、**その配備で「正しく使っている」報告が出たらこの 1 文字だけを外す**。実挙動は `BracketingANonIpv6Entry_SilencesTheWarning_WithoutMakingTheHostReachable` が固定する。**ただし統合テストの harness（`WebApplicationFactory`）は Kestrel を通さないので、そこでの「実測」は本番の `Host` ヘッダーの受け付け方とは違う** —— 実際 TestServer は `Host: [www.example.test:8080:]` を 200 で通す（本物の Kestrel は 400）。**この食い違いに気づかずに TestServer の結果を根拠にすると、判定を緩める方向の誤りを書く**（実際に一度そう書いた）。Kestrel の実測値は `ContainsSpellingAHostHeaderCannotCarry` の docstring が正本。**「直すと全許可になる項目」はさらに別の理由として先に名乗る** — `incident.example.com;0.0.0.0:8080`（`ASPNETCORE_URLS` を写すと自然に生まれる形）は*消す*だけなら安全なので分類は `Safe` だが、警告が実際に勧める*書き直す*操作に従ってポートを外すと `0.0.0.0` になり、ホスト名の絞り込みが丸ごと無効になる（issue #64 へ移る）。**「直したらどうなるか」は運用者が書いた綴りから数え上げ、判定はフレームワークと同じ項目の正規化（`ToUriComponent`）で行う**（issue #269）—— ホスト部（`HostString.Host`）で見ると `::` が `[::]` へ化け、`" ::"` の空白を外すと全許可だと**事実と違うこと**を言う（実測では `::` はどのホストも 400）。出発点を正規化後にするのも同じ誤りで、正規化自体が角括弧を補うため運用者がしない直しを数えてしまう。**直し方のモデルは原理的に完全にならない**ので、どの理由の文面にも「直した結果が `*` / `[::]` / `0.0.0.0` なら書くな」の注意を入れて backstop にしてある。**手は 1 つずつ「外すとテストが落ちる」ことを確かめて持つ** —— 外しても落ちない手は、ほかの手に覆われているか、足した理由そのものが（実測で）消えている。ただし**corpus で差が出ないことだけを根拠に減らさない**: 外側の角括弧だけを外す手は 9,549 通りで差が 0 でも `[[::]]` を落としうるので、**その手が唯一届くはずの綴りを構成して確かめてから**減らし、確かめた綴りはテストへ残す（`[[::]]` ・ `[[0.0.0.0]]` ・ `0.0.0.0:99999` ・ `[::]:99999`）。**`DeadEntryDeletionOutcome` は「消したら何が起きるか」しか見ていない**ので、直した結果は `RepairedSpellings`（**空白をすべて**落としてホスト部だけにした綴り。下記のとおり直し方は複数）で見て、ワイルドカードなら `WildcardOnceRepaired` として「そのまま直すな」と名指しする。**落とすのを前後の空白だけにしない**（レビュー指摘） — 正規化は角括弧を*補う*ので空白は**括弧の内側へ移りうる**（`" ::"` → `"[ ::]"`。区切りのうしろに空白を入れた一覧で、`ASPNETCORE_URLS` から写す `0.0.0.0` の IPv6 版）。`Trim()` だけだと内側の空白が残って `[::]` と一致せず、**この項目だけがワイルドカードの警告から外れて**「実ホスト名へ直せ」というごく普通の案内になり、従うと `::` ＝全許可（issue #64）——**空白 1 つで専用警告が有害な案内へ入れ替わる**（前後の空白でも同じ形になり、こちらは issue #256 以前から存在した）。**理由は項目ごとに添え、警告の文面に 1 つの原因を決め打たない** — 決め打つと、理由を足した瞬間にその文が名指しした項目について事実と違うことを言い出す（対応表は `DeadEntryCauseMessage`、足し忘れと使い回しは `AllowedHostsPolicyTests` が enum から導いて落とす）。**判定は生の綴りではなく正規化後に対して行う** — フレームワークが `Host` と突き合わせるのは正規化後の値で、その正規化は綴りによっては空白を落とす。生の綴りを `Trim()` と比べると、**空白付きなのに実際は一致する**項目を「消してよい」と案内してしまい、従った運用者が自分で障害を作ることになる（**警告が障害を作る側に回る**形で、見逃しより重い）。正規化できない綴りはここでは数えず `IsPermissive` 側の `UnparsableEntry` に任せる（同じ項目に原因の違う 2 本を出さない）。**残る境界もある**（名指しできない綴りが 1 つある）が、いずれも**見逃す側**なので許容する。**どの綴りがどう正規化されるかの実測値は `AllowedHostsPolicy` の docstring が正本で、ここに書き写さない**（フレームワークを上げて再計測したとき、docstring とテストだけが直って CLAUDE.md に古い値が残る）。境界は `AllowedHostsPolicyTests` が、実挙動は `BracketedIpv6WithTrailingSpace_StillMatches_AndIsNotReportedAsDead` が固定する。**「消したら何が起きるか」の判定も並び順どおりに見る** — `Any` で「正規化できない項目があるか」を畳むと、前のワイルドカードで打ち切られて**実際には評価されない**項目まで「判断できない」に倒し、「ワイルドカードの項目も消せ」という本当に必要な案内を落とす。**警告へ載せる設定値・項目は、そのまま埋め込まず制御文字を可視化してから出す**（issue #258） — `UnparsableEntry` という分類がある時点でこの値には制御文字が入りうるので、CR / LF が混ざると**1 本の警告がログ上は複数のレコードに見え**、`docs/security.md` が案内する「警告が出ていないことの確認」が偽の継続行で破れる。可視化の規則（読めない文字を `\uXXXX`（BMP の外は `\UXXXXXXXX`）へ、逆斜線を `\\` へ）は `AllowedHostsPolicy` の 1 か所だけに置く（2 本の警告で書き写すと片方だけ直る）。**可視化の条件は綴りの表ではなく Unicode のカテゴリで決める**（`Control` / `Format` / `LineSeparator` / `ParagraphSeparator`） — 守りたいのはレコードが割れないことなので、効くのは行を割る文字かどうかで、`U+2028` / `U+2029` は **`char.IsControl` が `false`** なのに解析器やログビューアが行を分ける（同じ役割の `U+0085` は `true` で以前から通っていたため、**この 2 つだけが生のまま載る非対称**だった）。**手で並べた表は「今まで踏んだ分だけ」にしかならない** — `char.IsControl` ＋ `U+2028` / `U+2029` の形にしたあとも、同じ危険を持つ `Format` の文字が残っていた（実測）: `U+200B`（幅ゼロ空白）は**一致しえない項目を健全な項目と見分けられなくし**（`[ ]` で囲む意味が消える）、`U+202E`（書字方向の上書き）は**警告の行の残りを逆順に描かせる**ので、運用者が読む 1 行を別の内容に見せられる。**幅のある空白（`Zs`）は素通しにしてある** — `Zs` には普通の空白も含まれるので、カテゴリごと可視化すると普通の値が読めなくなる（空白として見えるぶん危険が小さい）。ガードのケースを CR / LF 1 つに絞るとこの非対称に気づけないので、`LineSeparatorsInTheValue_DoNotSplitTheWarningAcrossLogRecords` は 4 綴りすべてを通す。**テスト側が突き合わせる「行を割る文字」の一覧は、可視化の条件を呼ばずに手で持つ** —— 呼ぶと「可視化される文字は可視化されている」という自分自身との照合になり、条件を狭める変異を 1 件も検出しない。**対象は `AllowedHosts` の値だけではない** — 環境名（`ASPNETCORE_ENVIRONMENT` 由来）も同じ「運用者が設定する外部の文字列」なので同じ規則を通す（片方だけ通しても、もう片方がレコードを割る）。**どちらの警告も、起動時に 1 度だけでなく設定の再読み込みでも出し直す**（issue #264） — `appsettings.json` は既定で `reloadOnChange: true` で、`HostFilteringOptions` は `ConfigurationChangeTokenSource` 経由で再束縛されるため、**稼働中に `"*"` へ書き換えるとミドルウェアは即座に全許可へ切り替わる**。起動時のスナップショットしか見ないと、そのとき新しい警告が 1 本も出ず、`docs/security.md` の「2 本とも出ていないことの確認」が**そのまま誤った安心**になる（`*;real` を取りこぼしていた頃と同じ形の穴が、時間軸の方向に残っていた）。**引き金は設定の再読み込みトークンではなく `IOptionsMonitor<HostFilteringOptions>` の再束縛にそろえる** — 前者を直接見ると、フレームワークが追随をやめたときに**こちらだけが鳴り**、ミドルウェアは古い一覧のままなのに「全許可になった」と言い出す（警告が障害を作る側に回る形で、見逃しより重い）。**値は options ではなく設定から読み直す** — `HostFilteringOptions.AllowedHosts` は 0 件なら `["*"]` へ落とされた後の一覧なので、未設定のときだけ起動時と違う原因を名乗ることになる。**同じ値のまま再読み込みが起きても出し直さない**（設定のどこを直しても鳴るので、毎回出すと「本当に緩めた瞬間の 1 本」が同じ文面の山に埋もれる）。**警告の組み立ては `Program.cs` に書かない** — 呼び出し口が起動時と再読み込みの 2 つになったので、書き写すと片方にだけ理由や文面を足す変更が通る。正本は `Models/Validation/AllowedHostsWarningReporter`。**どちらの警告も、出す配線そのものは `AllowedHostsStartupWarningTests` が `Staging` で実際にアプリを起動して固定する** — 警告は `if (!IsDevelopment())` の中にあり、統合テストのフィクスチャは既定で `Development` として起動するため、これが無いと**ブロックを丸ごと消してもログの引数を入れ替えても全件緑のまま通る**（どちらも実測）。`Production` ではなく `Staging` を使うのは、`Audit:HashSalt` の必須チェックが `IsProduction()` 限定で、秘密鍵を持ち込まずに同じ分岐へ入れるため。**再読み込み経路の検査も同じ形にする**（設定プロバイダに直接「変わった」と言わせるので、ファイル監視の遅延を待たずに済み、呼び出しから戻った時点で購読側は走り終えている ——待ち時間の長短で結果が変わる検査はいずれ無効化される）。実測で、`OnChange` の購読を消すと再読み込み経路の 2 つが、重複抑止を外すと `ReloadingWithoutChangingTheValue_DoesNotRepeatTheWarning` 等が、握りの配線を消すと `AFailingLogSink_DoesNotPropagateOutOfTheReloadCallback` が落ちる（**件数は書かない** — 検査を足すたびに数字だけが古くなる。実際この文の「1 件」は、同じ PR の中で 2 件へ増えた時点で嘘になっていた）。**再読み込みのコールバックからは例外を通さない** — 変更トークンの発火は `CancellationTokenSource.Cancel()` 経由で例外を呼び出し元へ投げ直すので、本番ではファイル監視のスレッドで**設定ファイルに触れただけでプロセスが落ちる**。**打ち切りの範囲は「設定トークンに連なる全部」ではない** — `Cancel()` は `throwOnFirstException: false` なので登録された購読は最後まで呼ばれ、例外はまとめて投げ直される。打ち切られるのは**同じ `OptionsMonitor` のマルチキャスト**のほうで、`HostFilteringOptions` の購読はここと同じ連鎖に載っているため、**ミドルウェア自身の再束縛が走らず古い許可リストのまま残る**。**購読を張る 1 文自体も守る**（将来ホスト名フィルタを条件付きにすると `GetRequiredService` が投げ、警告を配線できないというだけでアプリが起動しなくなる）。**この経路には、§6 の「空の `catch` を作らない」に対する唯一の例外がある** — はしごの最下段（通常のログも標準エラーも落ちている状態）で、**残せる先がもう 1 つも無い**のに投げると、診断を残せないという理由だけでアプリが止まる（§9 と正面から衝突する）。握り潰しているのは「記録の失敗」であって業務上の失敗ではなく、**ここ以外へ広げない**（§6 は原本テンプレート同期領域なので、例外はこの §3 側に書く）。**起動時に出せなかった警告は、再読み込みが 1 度も来ない配備では出し直されない** — 環境変数で設定し設定ファイルを触らない配備（不変イメージなど）がその形で、そのときログに残るのは「検査に失敗した」の記録だけ。**`docs/security.md` の確認手順は「2 本とも無いこと」に加えて「`Failed to check AllowedHosts` と `Failed to subscribe to configuration reloads for AllowedHosts` が無いこと」も見る**（後者が出ていると、起動時の 1 回きりで**以降の再読み込みを一切拾わない**＝issue #264 の穴がそのまま残る。同じ変更セットで手順側も直してある）。**記録はアプリのログに出るとは限らない** — 出力先が落ちていれば標準エラーへ、それも書けなければどこにも残らない（アプリを止めないことを優先しているため）。**retry の代償も記録しておく**: 出力先が壊れたままだと、再読み込みのたびに「検査に失敗した」の記録が 1 本ずつ出る。警告そのものの重複は旗で抑えているが、**失敗の記録は抑えていない** — 壊れていること自体が異常なので出し続ける側へ倒している（頻度を絞るなら、その判断をここへ書き足すこと）。記録が失敗する場合に備えて最後の手段（別の出力先）まで置き、そこには**元の失敗も必ず一緒に出す**（出力先が落ちた理由だけを書くと、肝心の「再検査が失敗した」事実がどこにも残らない）。**現在値の読み取りも重複抑止と同じ錠の中で行う** — 読んでから渡す形にすると、再読み込みが立て続けに届いたときに「A が新しい値を読む → 横取りされ B がさらに新しい値で『前回と同じ』と判断 → A が古くなった値で警告して記憶を上書き」が起こりえ、**記憶が実際の設定とずれた結果、次に本当に緩めたときに重複抑止へ当たって 1 本も出ない**（issue #264 が塞いだ fail-open が競合の形で戻る）。この競合もテストからは再現できないので、**検出網ではなく API の形で防ぐ**: `AllowedHostsWarningReporter` は値ではなく**読み方（`Func<string?>`）**を受け取り、呼び出し側が読んだ値を渡す書き方そのものができない。**渡し方を「値」へ戻す差分はレビューで必ず止める。** **検査は起動時も再読み込みも同じ関数を通す** — 以前は再読み込み側にだけ例外の手当てがあり、起動時はログの出力先が落ちているだけで**アプリが起動できなくなる**非対称が残っていた（`AFailingLogSinkAtStartup_*` が落とす）。**残っている境界: 「購読を張ってから起動時の検査をする」という並び順には検出網が無い** — 逆順にすると、その 2 文の間に着地した再読み込みを拾う購読がまだ無く、しかも Reporter が値を覚えるので以降も出し直さない（ConfigMap の投影がその瞬間に着地して `"*"` へ変わると、全許可のまま 1 本も警告が出ない状態が固定される）。この競合はテストから再現できないので**規約とレビューで守る**（「検査してから見張る」は自然な書き方なので、並べ替える差分は必ず理由を確認する）。**retry は「値を覚えない」ことではなく、出せなかった側の旗で行う** — 覚えない形は retry にはなるが、片方が失敗し続ける限り**成功したほうの警告まで**再読み込みのたびに積み増され、「本当に緩めた瞬間の 1 本」が埋もれて重複抑止を置いた理由が崩れる。だから**値は成否によらず覚え**、`_permissiveFailed` / `_deadEntryFailed` が出せなかった側だけを次回へ持ち越す。**旗を「冗長だ」として消さないこと** — 消すと「出力先が一瞬落ちた瞬間の警告が永久に失われる」形へ戻る（両方向とも検出網がある: 旗を落とす変異と、値を覚えない変異がそれぞれ落ちる）。**残っている境界: 購読を張れなかったときの catch には検出網が無い** — `GetRequiredService<IOptionsMonitor<HostFilteringOptions>>()` を投げさせる手立てが無い（オプションの解決はアプリ全体が使うので、登録を外すと無関係な理由で起動しなくなる）。`throw;` へ戻す変異も、文面を逆の意味へ差し替える変異も全件緑のまま通るので、**ここは規約とレビューで守る**（隣の「購読を先に張る」並び順と同じ扱い）。**警告の文面は「絞れていない」だけでなく原因まで名指しする** — `IsPermissive` が `true` になる経路は 3 つ（1 件も残らない／ワイルドカード／正規化できない綴り）あり、**うち 2 つでは値にワイルドカードが 1 つも無い**ので、3 綴りを消せとだけ案内すると存在しないものを探させることになる（しかも正規化できない綴りでの実際の症状は全許可ではなく毎リクエストの例外）。原因は `ClassifyPermissive`、文面は `PermissiveCauseMessage` が返し、**どちらも `Program.cs` に書かない** — あそこは `if (!IsDevelopment())` の中なのでテストから 1 行も走らず、文面を反対の意味へ差し替えても全件緑のまま通る（実測）。原因ごとの文面の足し忘れ・使い回しは `AllowedHostsPolicyTests` が enum から導いて落とす（`switch` の `_` はコンパイルエラーにならないため）。写した既定設定の 2 行と境界の実測値は `AllowedHostsPolicy` の docstring が正本で、ここに書き写さない。 フレームワーク側の前提（3 綴りとも全許可になること）は `HostFilteringShortCircuitTests` が、判定の境界は `AllowedHostsPolicyTests` が固定する。`X-Forwarded-For` / `X-Forwarded-Proto` を信頼させるには `ForwardedHeaders:Enabled=true` ＋ `ForwardedHeaders:KnownProxies`。

### 見せ方（§15 の具体化）

- スタック形態は「DB/常駐サーバーが必要な Web」: README 冒頭のデモブロックは**デモ動画（GIF/mp4）必須**、公開 URL は任意（公開する場合は Render 等の無料枠＋PostgreSQL＋シードデータ＋定期リセットを整備してから。`Database:Provider=postgres` で切替可能）。
- 撮影対象スクリーンショット（`docs/screenshots/` 配下、シードデータのみ使用・PHI/実在の個人名厳禁）:
  1. `incident-list-default.png` — インシデント一覧（`/Incidents`）
  2. `incident-create-form.png` — 登録フォーム（なぜなぜ分析・予防策サブフォーム含む）
  3. `audit-log-list.png` — 監査ログ一覧（`/AuditLogs`）
  - 任意の追加候補: `dashboard-default.png` — ダッシュボード（KPI・再発アラート）
- デモ GIF: 「報告 → なぜなぜ分析 → 予防策 → 登録完了」の登録フロー 1 本（10MB 以下）。
- 撮影データは `DbSeeder.SeedDemoData`（Development のみ投入されるデモインシデント）＋Development のデモアカウント（§2）を使う。デモアカウントのパスワードをスクショに写さない。

---

## 4. 実装フロー（プランモード必須）

コード変更を伴う作業に着手する前に、**必ず Claude Code のプランモード（Plan Mode）で計画を作成し、ユーザーの承認を得てから実装に移る**こと。

- 対象: 機能追加・改修・リファクタ・バグ修正など、ソースコード／スキーマ／設定ファイルを変更するすべての作業。
- 計画には以下を含める:
  1. **Context**: 何を、なぜ変更するのか。正本（要件定義書・計画書）のどの項目に対応するか。
  2. **変更対象ファイル**: 修正・追加するファイルの絶対パス（既存ファイルは行番号や関数名まで具体化）。
  3. **再利用する既存実装**: 既にある関数・モジュール・Port を優先して再利用する。新規作成する場合はその理由を明記。
  4. **検証方法**: lint / typecheck / test（必要に応じて E2E）と、画面確認の手順。
- **例外**: タイポ修正・コメントのみの変更・1 行以下の自明な修正は計画作成を省略してよい。それ以外は必ず `ExitPlanMode` でユーザー承認を得る。
- 計画と異なる実装が必要になったら、いったん手を止めてプランを更新（または `AskUserQuestion` で確認）してから続行する。

## 5. コメント規約（最優先）

- **1 行ごとに初心者でも意味がわかるコメントを書く。** コード 1 行ごとに、プログラミング初心者でも処理内容が理解できる日本語コメントを付ける。変数宣言・条件分岐・関数呼び出し・ループ・`return` など、すべての実行行に対して「何をしているか」を説明するコメントを必ず添える（型定義の単純な再エクスポートなど明らかに自明な行は除く）。
- コメントは行の直前または行末に記述し、専門用語を使うときは平易な言い換えを併記する。
  - 例: `var x = users.Where(u => u.IsActive); // アクティブなユーザーだけを抜き出す`
- このルールは本方針固有であり、汎用的な「コメントは最小限に」というガイダンスよりも**優先**する。
- 言語別コメント記法: C# / JS / TS は `//`、Razor (`.cshtml`) は `@* *@`、JSON など非対応形式は対象外。

## 6. コーディング規約

- **既存コードのスタイルに合わせる。** インデント・命名・ファイル構成は周囲の慣習を踏襲する。
- **自己説明的な構成にする。** モジュールは単一責務に分割し、ファイル名・関数名から役割が推測できるようにする。公開関数には docstring / コメントで意図を残す。
- **設計判断を残す。** 非自明な実装やトレードオフには「なぜそうしたか」をコメントで残す。
- **定数・ラベルは一元管理する。** 配色・フォント・余白・UI 文言・enum ラベルなどは単一の参照元（例: `theme.py`, `constants.ts`, `EnumLabels.cs`, CSS の `:root` 変数）に集約し、各所に直書きしない。新しい値を追加したら参照元をすべて更新する。
- **マジックナンバー・マジック文字列を避ける。** 意味のある値（しきい値・キー名・パスなど）は名前付き定数にし、上記の一元管理に従って単一の参照元に置く。意図が読み取れない裸の数値・文字列をコードに散らさない。
- **重複を避ける（DRY）。** 同じロジックを書き写す前に既存の関数・モジュール・Port を探して再利用する（§4 のプラン段階で確認）。ただし将来を見越した過度な抽象化は避け、実際に 2〜3 箇所目で重複したら共通化する。
- **エラーを握り潰さない。** 例外は黙って捨てず、文脈を付けて再送出するかログに残す。空の `catch` / 裸の `except:` を作らない。回復不能な失敗は安全側に倒し（§9 の fail-closed）、ユーザーには内部詳細を含まない安全なメッセージを返す。
- **デッドコードを残さない。** 使われない import・変数・関数や、コメントアウトしただけの旧コードは削除する（履歴は Git に残る）。
- **変更は最小スコープに保つ。** 1 つの変更は単一の目的に絞り、無関係なリファクタや整形を同じ差分に混ぜない（§12 の「1 コミット = 1 論理変更」と整合）。レビューしやすい粒度を保つ。
- 言語別の補足:
  - **TypeScript**: `strict: true` を維持。`any` は禁止（不明なら `unknown`）。Props は `interface` で定義。パスエイリアス `@/*` → `src/*`。Server Component をデフォルトにし、必要時のみ `'use client'`。
  - **Python**: `from __future__ import annotations` を先頭に。公開関数に docstring。入力検証は「範囲外→クランプ、非数値→デフォルト」パターン。定数は `UPPER_SNAKE_CASE`。
  - **Bash**: 先頭で `set -euo pipefail`。ログは stderr。インデント 4 スペース。

## 7. アクセシビリティ（a11y）

- **対象は Web フロントエンド（HTML/CSS/JS を伴う UI）。** CLI・ライブラリ・バッチや、HTML を持たないネイティブ GUI（tkinter 等）には適用しない（共通規約の中で数少ない、適用範囲が限定される項目）。ネイティブ GUI の a11y は各リポジトリの固有ルールで扱う。
- **セマンティック HTML を使う。** 見出しは階層（`h1`→`h2`→…）を飛ばさず、操作要素は `<button>` / `<a>` を使う。`div` / `span` に `onClick` を乗せて疑似ボタン化しない。
- **キーボードだけで全機能を操作できるようにする。** フォーカス可能要素は可視のフォーカスリングを残す（`outline` を消す場合は代替の見た目を用意）。モーダルはフォーカストラップ＋`Esc` で閉じ、`tabindex` は `0` / `-1` のみ使う（正の値は使わない）。SPA でページ遷移したらフォーカスを新ページの先頭（`main` 等）へ移し、本文へ飛ぶスキップリンクも用意する。
- **スクリーンリーダーに情報を伝える。** 画像に `alt`（装飾画像は `alt=""`）、アイコンだけのボタンに `aria-label`、フォーム入力に対応する `<label>`（または `aria-labelledby`）を付ける。`aria-live` は簡潔な状態通知・エラーなど「即時に読み上げてほしい変化」に限って使う（検索結果・タイマー・カルーセル・ストリーミング等、頻繁に変わる領域全体に付けると読み上げ過多でかえって使いづらくなる）。
- **色だけに意味を持たせない。** エラー・成功などの状態はテキストやアイコンも併用する。コントラスト比は WCAG AA を満たす（通常文 4.5:1 / 大きな文字 3:1。加えて UI 部品の境界・状態・フォーカスリングや意味のある図形などの非テキストは 3:1＝SC 1.4.11）。配色は §6 の一元管理（CSS の `:root` 変数・`theme`）側でコントラストも担保する。
- **動きを抑えられるようにする。** `prefers-reduced-motion` を尊重し、過度なアニメーションは無効化できるようにする。
- **言語属性と外部リンクを正しく設定する。** ルート要素の `lang` は実際の文書の言語（§1 で宣言した UI 言語）に一致させる。多くは日本語なので `lang="ja"` だが、UI が他言語のリポジトリやローカライズページでは、その言語を正しく指定する（発音・言語処理が支援技術の挙動を左右するため）。別タブで開く外部リンクには `rel="noopener noreferrer"` を付ける。
- **検証する。** Lighthouse / axe などで a11y を確認し、可能なら CI（§14）に組み込む。キーボードのみでの操作確認を手動チェックに含める。

## 8. パフォーマンス・リソース

- **N+1 クエリを避ける。** ORM では関連を eager-load（EF Core の `.Include`、Prisma の `include` / `select`）でまとめて取得する（§E の `.Include(x => x.Incident)` と整合）。ループの中で 1 件ずつクエリを投げない。
- **よく絞り込む列にインデックスを張る。** `where` / `order by` / `join` で頻繁に使う列（ユーザー ID・日付・ステータス等）には DB インデックスを作成し、全件走査（sequential scan）を避ける。複合条件では複合インデックスの列順も意識する。
- **一覧取得は必ず上限・ページネーションを持たせる。** 件数無制限の取得をしない（§9 の DoS・リソース枯渇防止と整合）。既定件数・最大件数は定数で一元管理する（§6）。
- **計測してから最適化する。** 推測で最適化せず、プロファイラ／メトリクスで遅い箇所を特定してから手を入れる。早すぎる最適化で可読性を犠牲にしない。
- **重い処理で UI／イベントループを止めない。** 大量データの計算・変換・パースは分割や非同期化（Web Worker・ストリーム処理・バックグラウンドジョブ）で行い、メインスレッドやリクエスト処理をブロックしない。
- **フロントの配信を最適化する。** Web ではバンドルを分割（dynamic import / code splitting）し、画像は適切なフォーマット・サイズで最適化する。フォールド下や重要でない画像は `loading="lazy"` で遅延読み込みし、ファーストビューの LCP 候補（ヒーロー画像等）は遅延させず優先的に読み込む（Next.js は `next/image`。LCP 画像の優先読み込みはバージョンに応じて指定する: 16+ は `preload`、〜15 は `priority`）。重い依存は遅延読み込みする。
- **Core Web Vitals を意識する。** LCP / CLS / INP を悪化させない。画像・広告枠などにはサイズを指定してレイアウトシフトを防ぎ、Web フォントには `font-display: swap`（または `fallback`、装飾用途は `optional`）を設定して FOIT（文字が一定時間不可視になる現象）で LCP を悪化させない。
- **キャッシュを活用する。** 同じ計算・取得を繰り返さない（メモ化やフレームワークのキャッシュ機構 — Next.js なら既定は `unstable_cache`、16+ で `cacheComponents` を有効にしている場合のみ `use cache`、§D と整合 — を使う）。キャッシュは無効化条件を明確にし、古いデータを返さないようにする。
- **リソースを確実に解放する。** 接続・ファイル・タイマー・購読は使い終わったら閉じる（§D の SSE 購読のように、購読解除を必ず実装する）。

## 9. セキュリティ（必達）

- **入力は信用しない。** 外部入力（ユーザー入力・設定ファイル・JSON・環境変数）は必ず検証する。Web ではスキーマ検証（例: Zod の `safeParse`）を通してから永続化・API へ渡す。壊れたデータでクラッシュさせず、不正値はフォールバックする。
- **正規表現で algorithmic DoS（ReDoS）を起こさない。** 信頼できない入力に使う正規表現はネストした量指定子（`(a+)+` など）を避け、入力長に上限を設ける。複雑なパターンは線形時間のマッチャや専用の検証ライブラリに寄せ、未検証の正規表現を外部入力に直接当てない。
- **秘密情報をコミットしない。** 認証情報・トークン・API キー・個人情報をコード・ログ・コミットに含めない。`.env` / `.env.local` はコミットせず、`.env.example` にキー名だけ記載する。
- **API キーをフロントエンドに露出させない。** 外部 API はサーバー側（API ルート / Server Action）経由で呼ぶ。モデル名やエンドポイントは定数・環境変数で管理し、ハードコードしない。
- **認可はサーバー側で強制する。** UI を隠すだけに頼らず、Server Action / コントローラの冒頭で認証・ロールチェックを行う。マルチテナントではクエリに必ずテナント条件（`where.tenantId = session.user.tenantId`）を差し込み、クロステナント漏洩を防ぐ。
- **外部 Webhook は署名を検証する。** 受信 Webhook は共有シークレットで HMAC 署名（例: `X-Hub-Signature-256`）を検証し、一致しないリクエストは拒否する。アプリ層の認証だけに頼らず、なりすまし POST で状態を変えられないようにする。検証は定数時間比較で行う。
- **危険な実行・安全でない解析を避ける。** `eval` / `exec` / `pickle` / `shell=True` を使わない。外部コマンドは引数配列で実行し、ユーザー入力を文字列連結でシェルに渡さない。信頼できない XML は外部実体（XXE）・DTD を無効化したパーサで読み（.NET は `DtdProcessing = Prohibit`、Python は `defusedxml`）、YAML は `safe_load`、JSON は eval せず標準パーサで解析する。
- **失敗しても安全側に倒す（fail-safe / fail-closed）。** 例外時はクラッシュや権限昇格ではなく機能を縮退して継続する。権限・ネットワーク・パスの判定は「不明なら拒否」をデフォルトにする。
- **最小権限・最小公開。** 読み書きするファイルは想定パス配下に限定し、外部由来の値をそのままパスに連結しない（パストラバーサル防止）。
- **サーバー側の外向きリクエストを検証する（SSRF 対策）。** ユーザー由来の URL をそのまま `fetch` / HTTP クライアントに渡さない。スキーム・ホストを許可リストで制限し、プライベート IP（`127.0.0.0/8` / `10.0.0.0/8` / `169.254.0.0/16` 等）やクラウドメタデータ（`169.254.169.254`）への到達を遮断する。リダイレクト追跡先も同様に検証する。
- **リダイレクト先を検証する（オープンリダイレクト対策）。** `returnUrl` / `next` など外部由来の遷移先は、自サイト内パス（または許可リスト）に照合してからリダイレクトする。任意の絶対 URL へ飛ばさない（ログイン後フィッシング防止）。
- **出力もエスケープする（インジェクション対策）。** SQL は ORM／パラメータ化クエリで組み立て、ユーザー値を文字列連結で SQL に混ぜない。HTML はフレームワークの自動エスケープに任せ、`dangerouslySetInnerHTML` / `v-html` / `innerHTML` などの生 HTML 挿入は原則避ける（やむを得ない場合はサニタイズしてから）。OS コマンド・パス・LDAP なども同様に値を直接連結しない。
- **状態変更リクエストを保護する。** フォーム送信や書き込み系 API には CSRF トークン（またはダブルサブミットクッキー）を必須とする。`SameSite` クッキーはそれを置き換えるものではなく、多層防御として併用する（OWASP 準拠。登録ドメインを他サービスと共有する場合、サブドメイン経由や `Lax` のトップレベル遷移では `SameSite` だけでは防げない）。副作用のある操作を GET で行わない。
- **機密情報・PII・スタックトレースをログやエラー応答に漏らさない。** 外部にはサニタイズした安全なメッセージだけを返し、詳細はサーバ内ログに限定する。ログに残す前にトークン・個人情報はマスク／伏字化する。
- **暗号・認証情報は自前実装しない。** パスワードは平文・可逆形式で保存せず bcrypt / argon2 等でハッシュ化する。暗号化は標準ライブラリを使い、自前の暗号方式を発明しない。署名・擬似匿名化に使うソルトや鍵は環境変数から取得し、未設定なら起動を失敗させる（fail-closed）。
- **依存（サプライチェーン）を管理する。** ロックファイルをコミットし、新規依存は最小限に絞って出所・メンテ状況を確認する。`npm audit` / Dependabot 等で既知脆弱性を定期的に確認し、放置しない。
- **公開エンドポイントを保護する。** レート制限と、リクエストサイズ・タイムアウト・ページネーション上限を設けて DoS とリソース枯渇を防ぐ。

## 10. 移植性・プラットフォーム差異ゼロ設計

- **ロジックと UI を分離する。** ビジネスロジックは表示層に依存しない純粋関数として保ち、Web / デスクトップ / モバイルで共有できる状態を維持する。
- **プラットフォーム差を 1 か所に閉じ込める。** OS / 実行環境固有の処理は分岐（例: `platform.system()`）で局所化し、必ずフォールバックを用意する。
- **移植可能な書き方をする。** 特定 OS でしか動かない記述（例: `strftime` の `%-d`）を持ち込まない。同じ操作はどの環境でも同じ結果になるよう、判定ロジックは共有層に置きテストで担保する。
- **契約（contract）で同一性を保証する。** 言語・プラットフォーム非依存の入力→期待出力ケース（例: JSON の契約ファイル）を真実の源とし、各実装はそれに従う。

## 11. テスト

- **テストは必ず通過させること。** 変更の前後で、そのリポジトリの §2 と CI 設定（`.github/workflows/`）に記載された実際の検証コマンド（lint / 型チェック / テスト等、存在するものすべて）を通す。コマンド名や有無はスタックごとに異なるため、§2 と CI 設定を正本とし、ここに書かれた例（`lint && typecheck && test` 等）をそのまま当てはめない。
- テストファイルはそのスタックの慣習に従った場所・命名で配置する（例: Python/JS は `tests/` に `test_<module>.py` / `*.test.ts`、Maven は `src/test/java` に `<Class>Test.java`、.NET は専用テストプロジェクトに `<Class>Tests.cs`）。
- **純粋ロジックはユニットテスト、DB / 外部依存は E2E or 契約テストに寄せる。** ユニットテストに DB アクセスを持ち込まない。外部 API はモックして実際には呼ばない。
- 境界値（0・最大・空文字列・非数値など）を重視する。OS 依存処理はモック（`@patch` 等）し、特定 OS でしか通らないテストを作らない。
- DB を破壊的に扱う契約テストは、専用 DB を明示フラグで起動したときだけ走らせ、開発 DB を指さない。共有 DB を `TRUNCATE` するテストは直列実行する。

## 12. Git 規約

- コミットメッセージ形式: `type(scope): 日本語の説明`
  - type: `feat` / `fix` / `refactor` / `test` / `docs` / `chore`
  - scope: 変更領域（例: `chat`, `api`, `ui`, `tickets`, `reminder`）
  - 例: `feat(chat): ストリーミング応答の実装`
- **1 コミット = 1 論理変更。** スキーマ変更とマイグレーションは同一コミットに含める。
- 開発は機能ブランチで行い、`main`（デフォルトブランチ）への直 push は避ける。

## 13. PR・レビュー運用

- **PR は draft ではなく open（ready）で作成する。** harness のデフォルトが draft の場合は、作成直後に ready 化してから次の手順に進む。
- **コードレビューは `/code-review ultra` と `/security-review ultra` で行う（Codex 自動レビューは廃止）。** PR を ready 化して open にした直後、および PR ブランチへ push するたび（初回 PR 作成時を含む）に、この 2 つのスキルを実行して差分をレビューする。`@codex review` コメントの投稿は行わない（`chatgpt-codex-connector` 連携には依存しない）。
  - `/code-review ultra` — 差分の正確性（バグ）と、再利用・簡素化・効率・粒度（altitude）の観点でレビューする。
  - `/security-review ultra` — ブランチの保留中変更に対してセキュリティレビューを行う。
  - 指摘は対応可否を判断して反映し、対応・見送りの理由をチャットで報告する。質問返信やレビュー不要な状況報告では実行しない。
- **CI の成否（グリーン）はチャット上で報告する。** GitHub MCP（check-runs / status）で取得して報告する。
- **ユーザーへの結果報告・要約は必ず日本語で出力する。** CI の成否・レビュー結果・作業サマリなど、要約した結果は常に日本語で記述する（各リポジトリの UI／コード言語に関わらず、チャットでの報告は日本語に統一する）。

## 14. CI

- GitHub Actions（`.github/workflows/`）でそのリポジトリに必要な検証を実行する。実行するジョブはスタックに応じて異なる（例: Web/TS なら lint → typecheck → test → E2E、Maven なら `mvn -B verify`、.NET なら `dotnet build` ＋ `dotnet test`、シェル/Docker なら shellcheck / hadolint / `docker compose config` ＋ e2e）。**PR を出す前に、§2 と CI 設定に記載のローカル検証コマンド（そのリポジトリに実在するものすべて）を通す。** 存在しないコマンドを当てはめない。
- DB / ブラウザ依存のジョブはサービスコンテナ（PostgreSQL 等）や chromium を使う。依存があるスタックでは、ローカルでも `docker compose up` などで依存を起動してから実行する。

## 15. 見せ方（ショーケース）

> **成果物は「動くコード」だけでなく「伝わる見せ方」までを完成の定義に含める。** README を開いて 5 秒で「何のアプリで、どんな画面か」が伝わらない状態を不合格とする。

- **README 冒頭に「デモ」ブロックを必須で置く（UI を持つ全リポジトリ）。** 概要の直後に次の 3 点を配置する: (1) 公開デモ URL（バッジまたはリンク）、(2) 主要画面のスクリーンショット 3〜5 枚、(3) 代表的な操作フロー 1 本のデモ GIF/動画。CLI・バッチはスクショの代わりに端末操作の録画（asciinema / GIF）を置く。
- **公開デモ URL は段階必須とする。** 無料枠で維持できる形態は URL を必須、維持コストが発生する形態は動画で代替可とする（規約の形骸化を防ぐため強制度を分ける）。

  | スタック形態 | ルール | 手段の例 |
  |---|---|---|
  | 静的サイト | URL **必須** | GitHub Pages |
  | serverless で完結する Web | URL **必須** | Vercel 等の無料枠。公開前にレート制限・利用上限を設定（§9 と整合） |
  | DB/常駐サーバーが必要な Web | デモ動画 **必須**、URL は任意（推奨） | Neon・Render 等の無料枠＋シードデータ＋定期リセットを整備できる場合に公開 |
  | CLI / バッチ / サンドボックス | 録画で代替（URL 適用外） | asciinema / 端末操作 GIF |
  | ネイティブ GUI（tkinter 等） | スクショ＋GIF **必須**（URL 適用外） | OS のスクリーンショット / 画面録画 |

- **画像は保存場所・命名・品質を一元化する。** `docs/screenshots/` 配下に `<画面名>-<状態>.png`（例: `incident-list-default.png`）で置く。幅 1280px 目安、GIF は 10MB 以下に最適化し（§8 の配信最適化と整合）、すべての画像に日本語の `alt` を付ける（§7 と整合）。
- **スクショにはシード/ダミーデータのみ使う。** 実データ・PII・秘密情報・実在のメールアドレスを写さない（§9 と整合）。デモ用シードデータの投入コマンドは §2 に記載する。
- **UI 変更 PR では該当スクショを同一 PR で更新する。** コードとドキュメントの乖離を許さない原則（§3）をスクショにも適用する。変更した画面が写っている分だけ更新すればよく、全量の再撮影は不要。
- **撮影は自動化を推奨する。** Web 系リポジトリはシードデータ入りの画面を Playwright 等で自動撮影するスクリプト（例: `scripts/capture-screenshots.ts`）を用意し、実行コマンドを §2 に記載する。非 Web GUI は手動撮影でよい。
- **デモ環境は本番と分離し fail-safe にする。** デモアカウントは閲覧専用の最小権限ロール、環境変数・シークレットは本番と共有しない、書き込みは無効化または定期リセットする（§9 と整合）。
- **撮影対象画面・デプロイ先はリポジトリごとに具体化する。** 各リポジトリの `CLAUDE.md`（§1〜§3）に「どの画面を何枚撮るか」「どこへデプロイするか」を明記し、この章はその共通基準として使う。

---

## 付録: リポジトリ別のルール（Appendix）

各リポジトリの `CLAUDE.md` から、**§4〜§15 の共通規約と重複しない固有ルールだけ**を抜き出したカタログ。
新規リポジトリが似た技術スタックの場合、該当ブロックを §1〜§3 の具体化や追補として流用できる。
（出典の `CLAUDE.md` には、ここに載らないプロジェクト固有のアーキテクチャ詳細も含まれる。詳細は各リポジトリを参照。）

### A. profile-portfolio（静的 HTML/CSS/JS, GitHub Pages）

- **サイト本体にページ生成のビルドステップは無い**（`index.html` / `resume.html` をそのまま配信する）。表示確認は `open index.html` または `python -m http.server 8000`。
- 一方で検証・撮影用に npm ベースの開発ツール（html-validate / Playwright ビジュアルリグレッション / Lighthouse CI / スクショ自動撮影）を持つ。`package.json` ・ `package-lock.json` はコミットし、検証は `npm ci` の後に CI と同じコマンド（`npx html-validate` / `npm run test:e2e` / `npm run test:lighthouse`）で流す。
- ファイル構成: `index.html`（ダークテーマ）/ `resume.html`（ライトテーマ・印刷対応）/ `data/portfolio.json`（表示データ）/ `e2e/`（ビジュアルリグレッション）/ `scripts/`（スクショ・デモ GIF の自動撮影）。
- 色の変更は `:root` の CSS 変数（`--primary`, `--accent`, `--bg-dark` 等）経由。個別要素にカラーコードを直書きしない。
- レスポンシブ: ブレークポイント 968px（タブレット）・768px（モバイル）。グリッドは `auto-fit, minmax()` でメディアクエリを最小化。フォントは `clamp()` で流体タイポグラフィを適用。
- CSS は BEM 風命名（`.section-header` 等）、JS は Vanilla のみ（外部ライブラリを追加しない）。a11y の基本（セマンティック HTML・`alt`・外部リンクの `rel`・`lang`）は §7 に従う（この repo は日本語 UI なので `lang="ja"`）。
- セクション追加時は Intersection Observer の `.observe()` 対象に追加し、ナビリンク・768px 表示・`resume.html` 反映を確認する。

### B. my-task-manager（Python + tkinter, GUI）

- ビジネスロジックは GUI 非依存の純粋関数に保つ（`timeline.py` / `stats.py` / `recurrence.py` / `task.py`）。表示層を差し替えてもロジックを共有できる状態を維持。
- デザイントークン（配色・フォント・余白・カレンダー寸法）は `theme.py` に一元化。見た目の値をコードに直書きしない。
- 繰り返しタスクは「完了した時点」を起点に日/週/月/年で再スケジュール（月末日・うるう年はクランプ）。
- 言語・プラットフォーム非依存の繰り返し契約は `contract/recurrence_cases.json` を真実の源とし、契約駆動テスト（`test_recurrence_contract.py`）で検証。Web/スマホ版も同一契約に従う。
- tkinter の `StringVar` / `IntVar` はテスト用 `_DummyVar` で代替し、`AppTestCase._app()` ファクトリ（`tests/test_planner.py`）でモック済みインスタンスを生成（Tk 無しでテスト可能）。
- 入力検証は `_coerce_int()` パターン（範囲外→クランプ、非数値→デフォルト）。
- 今日のタスクは Treeview でなく `tk.Canvas` の「デイビュー」で描画し、位置・高さは分→px 換算（`HOUR_HEIGHT`）で Canvas 実サイズに依存させない。
- クロスプラットフォーム: 音/通知は macOS(`afplay`)・Windows(`winsound`)・Linux(`notify-send`+`tk.bell()`)を `platform.system()` で分岐。`cairosvg` はオプション依存で `ImportError` 時 graceful degradation。

### C. my-first-ai-app（Next.js 16 + Claude API）

- システムプロンプトは `src/lib/prompts.ts` に集約し、コンポーネントやルートハンドラに直接書かない。プロンプトは日本語で記述。
- モデル名（`claude-sonnet-4-6` 等）は環境変数または定数で管理しハードコードしない。`max_tokens` 既定 1024、長文が必要なカテゴリは `prompts.ts` で個別設定。
- `POST /api/chat` が唯一の API。Claude へのストリーミングプロキシで、`ANTHROPIC_API_KEY` はサーバ側環境変数から取得しフロントに露出させない。
- API ルートに簡易レート制限（IP ベース、1 分あたり 20 リクエスト目安）。
- API ルートのエラーは HTTP ステータスを使い分け: 400（JSON 破損・入力検証エラー・上流 Claude API の 400）/ 401（キー未設定/無効）/ 413（本文サイズ上限超過）/ 415（`Content-Type` が `application/json` でない。**レート制限より前に**検証し、第三者サイトの simple request で被害者 IP のレート枠が枯渇するのを防ぐ）/ 429（レート制限超過。`Retry-After` ヘッダ付き）/ 499（接続確立前のクライアント切断）/ 500（その他）。文言は `route.ts` の `ERROR_MESSAGES` に一元管理し、フロントはユーザーフレンドリーな日本語メッセージを表示。

### D. helpdesk-hub（Next.js 16 / Prisma / Auth.js v5）

- 正本は `docs/smb-dx-pivot-plan.md`。Lite/Pro 二層・マルチテナント化・用語簡素化等の方針に反する変更をしない。Phase 0→1→2→3→4 の順序を尊重し、後フェーズ機能を前フェーズに混ぜない。
- Prisma クライアントは `src/generated/prisma` に出力される。型/enum は必ず `@/generated/prisma` から import（`@prisma/client` ではない）。クローン後やスキーマ変更後は `npm run db:generate` を実行。
- ロールは実質 requester と agent/admin の 2 種。`isAgent(role)`（`src/lib/role.ts`）を使い、`role === 'admin'` を直接比較しない（admin 限定の意図がある場合を除く）。
- Mutation（Server Action）の定型: `auth()`＋ロール表明 → `findUniqueOrThrow` → 状態変更は `isValidTransition(from,to)` でゲート → Prisma で更新 → `recordHistory(...)` → `createNotification(...)` → `revalidatePath(...)`。
- 状態遷移テーブル `ALLOWED_TRANSITIONS`（`src/domain/ticket-status.ts`）が唯一の真実の源。バイパスせず、ブロックされたら表とテストを更新する。
- Data 層は Ports & Adapters。Port を `src/data/ports/` に定義し、本番は `adapters/prisma/`、テストは `adapters/memory/`。Prisma を直接 import するのは Adapter 内のみ。
- 未読通知数は SSE（`/api/notifications/stream`）＋ `unstable_cache` で配信。`sse-subscribers.ts` はインプロセス Map のため、水平スケール前に要注意。
- 契約テストは `*.contract.prisma.test.ts` 命名。`RUN_PRISMA_CONTRACT=1` のときだけ走り、`beforeEach` で全テーブル `TRUNCATE` するため**開発 DB を指さない**。`--no-file-parallelism` で直列実行。

### E. incident-insight（ASP.NET Core 8 MVC + EF Core 9）

- DB プロバイダ非依存。SQLite（既定）/ SQL Server / PostgreSQL を `Database:Provider` で切替。プロバイダ固有 SQL・列型をコードに持ち込まない。
- 楽観的同時実行制御: 編集 POST は `FindAsync` で再読込後、クライアントの編集前 `ConcurrencyToken` を `OriginalValue` に明示ピンして保存し、`DbUpdateConcurrencyException` を捕捉。トークンは hidden field で round-trip。
- 時刻は常に注入された `IClock`（JST）。`DateTime.Now/Today/UtcNow` を直接呼ばない。
- 監査ログは `AuditSaveChangesInterceptor` が唯一の源。`AuditLog` に直接書かない。`SaveChanges` 経由で更新し、`ExecuteUpdate`/`ExecuteDelete` を使わない（変更追跡を迂回し監査漏れになるため）。
- PHI 保護: 自由記述・個人名カラムには `[Sensitive(Mask.Redact)]` か `[Sensitive(Mask.Hash)]` を付与（`[REDACTED]` か HMAC-SHA256 擬似匿名化）。新カラム追加時も必ず annotate。本番で `Audit:HashSalt` 空は起動失敗。
- ビジネスルール `HasAtLeastOneValidMeasure`（インシデントは予防策が最低 1 件ないと登録不可）をバイパスしない。
- Enum（重症度・部署・インシデント種別）は `Incident` クラスの `static readonly` 辞書/配列が真実の源（DB ではない）。`EnumLabels.cs` に日本語ラベル＋Bootstrap カラーを集約。
- `SameDepartmentHandler` は `Incident` の eager-load（`.Include(x => x.Incident)`）が前提で fail-closed（null なら拒否）。
- 新規 POST アクション時チェック: `[Authorize]` / `[ValidateAntiForgeryToken]` / `ConcurrencyToken` ピン / 必要なら `.Include(Incident)` / `SaveChangesAsync` 使用 / `TempData["Success"|"Warning"]` / 新 PHI カラムに `[Sensitive]` / 対応テスト追加。テストは InMemory DbContext を優先（Mock より）。

### F. AI-Docker-Environment（Docker サンドボックス, bash, Linux 専用）

- 正本は `docs/requirements.md`。実装・新機能はすべて要件定義書に従い、衝突したら先に要件を改訂してから実装変更（同一 PR 内で §3/§4/§6 を更新）。
- すべて `bin/aidock` 経由で実行（`build`/`login`/`run`/`shell`/`firewall-refresh`/`logout`）。`guard_workspace()` の `/` および `$HOME` をマウント拒否するガードを削除しない。
- セキュリティ不変条件（変更禁止 or 影響必須検討）:
  - `compose.yaml`: `cap_drop: ALL`（必要 cap のみ add）/ `no-new-privileges:true` / `read_only: true`＋最小 `tmpfs` / メモリ・CPU・PID 上限 / ホストパスの追加 bind mount 原則禁止（`~/.ssh` 等）。
  - `HOST_WORKSPACE` に既定値を付けない（`${HOST_WORKSPACE:?...}`）。`bin/aidock` 非経由の直接 `docker compose run` を fail-closed にする。
  - `Dockerfile`/`entrypoint.sh`: `sudo` を含めず、root 起動 → firewall 初期化後に `gosu agent` で降格。ワークロードは `agent` で実行。
  - `init-firewall.sh`: `iptables -P OUTPUT DROP` と `ip6tables -P OUTPUT DROP`（IPv4/IPv6 両方 default-deny）を維持。許可ホスト追加は最小限・理由を PR に明記。DNS は許可 nameserver 限定。
- OAuth トークンは名前付きボリューム `claude-home` に置き、ホスト FS や Docker イメージ層に書き出さない。
- Linux 専用（iptables/ipset/cap_add 依存。macOS Docker Desktop 非対応）。スクリプトは Bash・先頭で `set -euo pipefail`、ログは stderr、インデント 4 スペース。
- このリポジトリのコミットメッセージは英語・命令形・1 行要約（既存履歴に倣う。§12 の日本語コミット規約より優先する例外）。

### G. batch-scheduler（Java 21 / Maven, バッチ実行マネージャ）

- 正本は `docs/DESIGN.md`（アーキテクチャ・セキュリティモデル・将来拡張）。実装はここに従い、衝突したら先に設計を改訂してから実装を変更する。
- バッチ定義ファイル（YAML）は **Makefile / CI パイプラインと同等の信頼入力**として扱う。一方で資源枯渇には防御する: bounded YAML parsing、出力キャプチャの上限、反復的（再帰でない）グラフアルゴリズム、state ディレクトリの安全性（runId 検証・シンボリックリンク非追従）。
- MVP 非目標（non-goals）: スケジューリング・並列実行・分散。
- テストは `src/test/java/...` の各クラス対応（`BatchConfigLoaderTest` / `BatchExecutorTest` / `DependencyGraphTest` 等）。CI は `mvn -B verify`（Java 21 / Temurin）。

### H. Expense-Management-Rest-API（Java 21 / Spring Boot, REST API）

- Java 21 / Spring Boot 3.3.5 / PostgreSQL 16 / Maven / Docker の REST API 単一プロジェクト。アプリ一式（`pom.xml` ・ `src/` ・ `Dockerfile` ・ `docker-compose.yml`）をリポジトリ直下に置く。金額は `BigDecimal` を使い浮動小数誤差を避ける。レスポンスは JSON、エラー形式は `{ "status": int, "message": string }`、入力検証は Jakarta Bean Validation。
- 課題の棚卸しは `docs/issue-analysis.md`（機能面・セキュリティ面の分析）。
- 層構成: `controller/` → `service/` → `repository/`（Spring Data JPA）→ `domain/`（JPA エンティティ）。`dto/request/` と `dto/response/` を分離し内部エンティティを API 契約から切り離す。`GlobalExceptionHandler` がカスタム例外を HTTP ステータスへマップ。
- 横断的関心事は `web/`（エラー応答の共通整形・ページング入力の無害化・リクエスト本文サイズ上限）・`security/`（IP ベースのレート制限フィルタ）・`validation/`（コードポイント単位の文字数検証・カテゴリ名の NFC 正規化）に分ける。
- CI は `.github/workflows/ci.yml` の `build-test` ジョブ 1 本で `./mvnw -B verify`（Temurin JDK 21）を実行する。`repository/` 配下のテストは Testcontainers で PostgreSQL を起動するため Docker デーモンが必要。

### I. agent-ops（Next.js 16 / Prisma 7, Agent Ops SaaS）

- 正本は `docs/spec.md`（ユースケース・ER 図・API 一覧）と `docs/roadmap.md`（8 Step のロードマップと受け入れ基準）。実装と衝突したら先に文書を改訂してから実装を変える。設計判断は `docs/adr/` に ADR として残す。
- **各 Step の受け入れ基準は `npm run gate:stepN` として自動化し、`main` でゲートが緑になってから次 Step のブランチを切る。** 基準を緩める変更は `docs/roadmap.md` と該当 ADR を同じ PR で更新する（テスト側だけを書き換えない）。Step の順序（0→1→…→7）を入れ替えず、後 Step の機能を前 Step に混ぜない。
- REST API は `openapi/openapi.yaml`（OpenAPI 3.1）が契約の正本。`npm run gen` が `src/generated/openapi.d.ts` に型を生成し、`src/lib/api-types.ts` がアプリ側の名前で再公開する。新しいエンドポイントは「定義 → `gen` → 実装 → API テスト」の順で作る。
- マルチテナントは行スコープ（全テーブルに `tenantId`、ADR-0002）。他テナントの資源は 404 で隠す（403 だと存在が漏れる）。
- RBAC は `viewer` / `operator` / `admin` × `view` / `execute` / `stop` の許可表 `src/domain/rbac.ts` が唯一の真実の源（不明なら拒否）。
- Prisma クライアントは `src/generated/prisma` に出力される。enum の正準は `src/domain/types.ts`（`as const` で定義し Prisma の実行時コードに依存しない。Prisma 側との一致はテストで固定）。`@/generated/prisma` の直接 import は ESLint が禁止し、例外は結線箇所の `src/lib/prisma.ts` / `src/lib/prisma-client.ts` と prisma アダプタ `src/data/adapters/prisma/` だけ（データ層は Ports & Adapters。契約 `src/data/ports/`、本番 `adapters/prisma/`、テスト `adapters/memory/`。ADR-0006）。結線は `createPrismaClient()` に集約。生成物（`src/generated/`）はコミットしない。
- 金額はマイクロ USD の整数（`BigInt`）で持ち、JSON では文字列で運ぶ。API キーは SHA-256 ハッシュ（`keyHash`）と先頭数文字（`prefix`）だけを保存し、平文は発行応答でしか返さない。
